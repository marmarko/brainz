{"id": "stack.shared-normalizer", "capability": "Shared NFKC + lowercase + whitespace normalizer applied on write AND read, so the alias table is queried the way it was populated", "criticality": "critical", "status": "covered", "unit": "U5", "source": "accuracy-stack", "notes": "U5 ships it as one module: `src/core/write/normalize.ts` (NFKC + typographic fold + lowercase + whitespace) is re-exported by `src/core/search/normalize.ts`, so both sides call the same function objects — `test/core/search/normalize.test.ts` asserts function identity across the seam and `test/core/write/normalize.test.ts` scans `src/` for a second NFKC call site. The last read path that bypassed it no longer does: `src/mcp/reads.ts:entityCard` folds the asked name through the same re-export and compares the key against the columns the write path wrote keys into (`entity_alias.alias`, `entity_slug.slug`), so a name carrying a curly apostrophe, a fullwidth letter or an ellipsis resolves identically through `entity` and through `recall`. `test/mcp/entity-resolution.test.ts` asserts that parity rather than the tool's behaviour in isolation, since a second normalizer of its own would satisfy the latter."}
{"id": "stack.intent-classification", "capability": "Zero-LLM intent classification used as a ranking input, not only as a router — sets INTENT_WEIGHTS, effective RRF k, exact-match boost and recency tilt", "criticality": "critical", "status": "covered", "unit": "U5", "source": "accuracy-stack", "notes": "`src/core/search/intent.ts` classifies with no model call and returns a `RankingPlan`, not just a label: arm weights, RRF k, exact-match boost, recency tilt, alias weight, calendar lift and the edge types the question asked about. `read.ts:recallArms` refines the plan with what entity resolution found and carries it on the outcome, so the ranking is scored under the plan the arms actually ran."}
{"id": "stack.vector-arm", "capability": "Vector recall arm with asymmetric query/document input types and an explicitly set hnsw.ef_search", "criticality": "critical", "status": "covered", "unit": "U5", "source": "accuracy-stack", "notes": "`search/arms.ts:vectorArm` runs inside `schema/vector-query.ts:withVectorScan`, which sets `hnsw.ef_search` from `candidatePoolFor` in the query's own transaction; the same pool sizes the LIMIT, so an arm asking for a page of results instead of a pool is one visible token. Asymmetry is KTD8's: `write/embed.ts:queryEncoding` against `documentEncoding`. Guarded behaviourally by `test/hazards/h1-ef-search-truncation.test.ts` against a real Postgres, not by a unit test."}
{"id": "stack.keyword-arm", "capability": "Full-text (tsvector) recall arm with the FTS language chosen per tenant at provision time", "criticality": "critical", "status": "covered", "unit": "U5", "source": "accuracy-stack", "notes": "`search/arms.ts:ftsArm` runs `websearch_to_tsquery` over the chunk tsvector with the page title admitted to recall at a 0.4 discount. The language is read per tenant (`readFtsLanguage` off `tenant_setting`) and never defaulted — provisioning substitutes the same value into the generated columns (`schema/fts-language.ts`), so the read queries the index the write built."}
{"id": "stack.graph-arm", "capability": "Relational/graph recall arm WITH its edge supply — typed edges actually produced, not just an arm that can read them", "criticality": "critical", "status": "covered", "unit": "U5", "source": "accuracy-stack", "notes": "Both halves, which is what this row asked for. The arm is `search/arms.ts:graphArm` — two ordered hops out of the resolved seeds over `entity_edge`, ranked above both hops by whether a fact names both endpoints of an edge whose type the question asked about. The edge supply is U4's: `core/write/links.ts:reconcileEdges` projects typed edges from live facts inside the write transaction."}
{"id": "stack.alias-hop", "capability": "Read-path alias hop with exact-title and slug-suffix resolution precedence — the only layer bridging true synonyms with zero surface overlap", "criticality": "critical", "status": "covered", "unit": "U5", "source": "accuracy-stack", "notes": "`src/core/search/alias-hop.ts` — a closed set of five weighted rungs, exact title first, then slug-suffix, down to a bare mention, folded into the fusion by `rrf.ts:foldRanked` so a rung can inject a candidate no arm recalled. Implemented, not proven: `family.alias.hit1` is a `deferred` floor while the committed vectors are synthetic, so it reports 0.8571 against 0.98 and enforces nothing."}
{"id": "stack.rrf-fusion", "capability": "Reciprocal-rank fusion (K=60) combining the recall arms", "criticality": "critical", "status": "covered", "unit": "U5", "source": "accuracy-stack", "notes": "`src/core/search/rrf.ts:fuse`, weighted per arm. k is set by the plan rather than fixed at 60 — 60 is the exploratory value and lookup/relational queries take a sharper curve (`intent.ts:PLANS`) — which is the ranking role the intent row above asks the classifier for."}
{"id": "stack.cross-encoder-rerank", "capability": "Bounded cross-encoder rerank over the fused candidate pool", "criticality": "critical", "status": "not-yet", "priority": "p0", "unit": "U12", "source": "accuracy-stack", "notes": "Enabled in code at U12 and NOT covered: the stage has never run against a real cross-encoder. `src/core/search/rerank.ts:RERANK_DEFAULT_ENABLED` is true, the scorer is the gateway's `rerank` op issued from `core/search/read.ts:scoreWithCrossEncoder` over the packed candidate set and bounded by `rerank-stage.ts:RERANK_CANDIDATES_DEFAULT`, and a reranker that refuses degrades to `rerank_unavailable` — so the wiring is done and the cost is receipted at 125 micro-USD/query inside KTD4's envelope. What is missing is the only thing that would make `covered` true rather than merely built: the one measurement that exists, U12's own A/B at `evals/receipts/u12-rerank-ab.json`, is -0.1608 nDCG@10 — a DEGRADATION — and the receipt correctly marks its own `uplift_status: deferred` because every committed (query, candidate) score is synthetic and stage 12 sorts by that score alone, so the A/B measured a thirty-line lexical stand-in rather than `@cf/baai/bge-reranker-base`. `gap.read-path-model-spend` is not a precedent for covering this: that row is a settled DESIGN question with no measurement pointing the other way. A capability whose single number says it makes retrieval worse is not covered. WHAT FLIPS THIS ROW: provider-sourced cross-encoder scores in the rerank manifest — `score_sources.provider > 0` in `evals/receipts/u12-rerank-ab.json`, which turns `uplift_status` to `measured` with no edit — showing a non-negative delta, plus KTD4's request-time p99 from a deployed container (also deferred there, with what would produce it)."}
{"id": "stack.autocut", "capability": "Autocut result sizing on score discontinuity, reading rerank score only", "criticality": "critical", "status": "not-yet", "priority": "p0", "unit": "U12", "source": "accuracy-stack", "notes": "Wired at U12 and NOT covered, for one reason and it is not its own: autocut cuts on the cross-encoder score and nothing else, so it is exactly as unexercised in production shape as `stack.cross-encoder-rerank`. The coupling is structural rather than conventional — `rerank-stage.ts` resolves both stages as one decision, so `plan.autocut === plan.rerank` for every configuration — which means a synthetic rerank score makes every autocut decision a decision about the stand-in. The one measurement that exists, `evals/receipts/u12-rerank-ab.json`, is -0.1608 nDCG@10 against the shipped stack, `uplift_status: deferred`. The mechanism itself is built and its dials are reasoned: KTD4's named dial is the candidate count, `reduceForLatency` shrinks the pool to a floor and reports `at_floor` rather than returning a plan with rerank off, and `AUTOCUT_MINIMUM_KEPT` is 3 rather than 1, derived from R6's dilution Hit@3 floor — at a 0.5 relative-drop threshold a cut at position one is the ordinary case for a top-heavy distribution, which would make that floor unsatisfiable every time autocut fired. WHAT FLIPS THIS ROW: the same receipt as rerank — `score_sources.provider > 0` in `evals/receipts/u12-rerank-ab.json` — plus a result-size measurement over those provider scores, since the cut position autocut chooses is a property of the real score distribution and a stand-in's distribution says nothing about it."}
{"id": "stack.title-phrase-boost", "capability": "Title-phrase boost so a user typing a thing's literal name gets that thing", "criticality": "critical", "status": "covered", "unit": "U5", "source": "accuracy-stack", "notes": "`search/boosts.ts:titleTerm` — longest ordered phrase run over the title, a step rather than a ramp for full containment, and two refusals that stop it paying a resolved name or a bare residual a second time. What the corpus does and does not show is recorded: `test/core/search/mechanism-sensitivity.test.ts` finds ten of the twenty title-substring probes answered by the ladder's exact-title rung before this stage runs, so that family floor is evidence for the other ten only."}
{"id": "stack.recency-decay", "capability": "Per-prefix recency decay (14-60 day half-life on churn content)", "criticality": "critical", "status": "covered", "unit": "U5", "source": "accuracy-stack", "notes": "`search/boosts.ts:RECENCY_HALF_LIFE_DAYS` — 14 days for chat through 60 for documents and notes, keyed on `source_type` because brainz has no slug prefix to key on. A tilt scaled by `plan.recencyTilt`, so a relational question gets none, and `now` is a parameter rather than the wall clock so cross-class ordering cannot drift as real time passes."}
{"id": "stack.source-type-priors", "capability": "Source-type boost and hard-excludes (curated outranks bulk) as the first line of defence against contradictions", "criticality": "critical", "status": "not-yet", "priority": "p0", "unit": "U5", "source": "accuracy-stack", "notes": "Half ships and the other half is refused on purpose. `search/boosts.ts:SOURCE_TYPE_PRIOR` and `TRUST_PRIOR` run on every ranked read, so a document outranks a line of chat at equal term match — KTD5 settled the design question this row records, admitting source type and trust as ranking inputs while access stays on `origin_context` alone. There are no hard-excludes: `MIN_ENVELOPE` floors the envelope so no combination of priors can annihilate a row, so curated outranks bulk but never excludes it."}
{"id": "stack.corroboration-boost", "capability": "Graph-adjacency and cross-source corroboration boosts — independent attestation raises rank", "criticality": "important", "status": "not-yet", "priority": "p1", "unit": "U5", "source": "accuracy-stack", "notes": "The mechanism ships and cannot fire anywhere. `search/boosts.ts:corroborationOf` implements R12a exactly — only `user_out_of_band` and `internal` corroborate — and `search/arms.ts:CHANNEL_BY_SOURCE_TYPE` is deliberately incapable of producing either, so every row on both substrates carries one derived attestation that corroborates nothing. `test/core/search/mechanism-sensitivity.test.ts` records the term as identically zero across the whole corpus, and four probes name a mechanism none of them grades. Not covered until something writes a `user_out_of_band` or `internal` attestation (U12/U14/U15)."}
{"id": "stack.compiled-truth-boost", "capability": "Compiled-truth boost (2.0x post-normalisation) over a canonical summary chunk, so the synthesis outranks the raw log", "criticality": "critical", "status": "not-yet", "priority": "p1", "unit": "U11", "source": "accuracy-stack", "notes": "U11 built the surface and the admission decision; the **boost itself is still absent**, which is why this stays not-yet. `runSynopsisPhase` materialises a canonical summary page per ingested page (`materialize.ts:writeCanonicalSummary`), marked `derivation = 'model_derived'` so no later cycle reads it back as evidence, and carrying `page.compiled_truth` — R12a's admission, decided once against the shared `corroborationOf` and stored rather than recomputed by a ranking stage that would have to re-refuse three known forgeries. What no stage does yet is *read* that column: U5's boost table has no compiled-truth term, so the synthesis does not outrank the raw log. That term is the remaining work and it belongs to the ranking stack."}
{"id": "stack.read-time-dedup", "capability": "Four-layer read-time dedup: top-3 per page, 0.85 Jaccard, 60% page-type cap, 2 chunks per page", "criticality": "critical", "status": "covered", "unit": "U5", "source": "accuracy-stack", "notes": "`src/core/search/dedup.ts` ships all four layers at the stated numbers (top-3 per page, 0.85 Jaccard, 60% page-type cap with a four-survivor floor, 2 chunks per page), run by `pipeline.ts` after the boosts and before packing. The dilution floor is not its evidence, twice over: it is `deferred` on synthetic vectors, and R6 defines it on the raw ranking so it cannot see dedup at all. `test/core/search/dedup.test.ts` is the measurement."}
{"id": "stack.return-policy", "capability": "Intent-adaptive return policy trimming the payload the caller pays for", "criticality": "important", "status": "not-yet", "priority": "p2", "unit": "U5", "source": "accuracy-stack", "notes": "Nothing intent-adaptive exists: `pipeline.ts` stage 11 is a flat `slice(0, limit)` and `RankingPlan` carries no return-size field, so the intent classifier that sets six other knobs sets nothing here. The ceiling that does exist is the caller's token budget (stack.token-budget-packing), which is a different lever."}
{"id": "stack.token-budget-packing", "capability": "Token-budget packing bounding the response payload", "criticality": "critical", "status": "covered", "unit": "U5", "source": "accuracy-stack", "notes": "`src/core/search/budget.ts:packToBudget` at `pipeline.ts` stage 11, reached from the surface through `recall`'s advertised `budget_tokens` parameter; `briefing` applies its own 4000-token default ceiling by dropping whole rows rather than truncating one. There is no server-side default on `recall`: absent `budget_tokens` the payload is bounded by `limit` (max 50) rather than by tokens."}
{"id": "stack.evidence-create-safety", "capability": "Evidence and create_safety on the response envelope, telling the writing agent whether the thing already exists", "criticality": "critical", "status": "not-yet", "priority": "p0", "unit": "U6", "source": "accuracy-stack", "notes": "The envelope carries no `evidence` and no `create_safety` (`mcp/envelope.ts:ENVELOPE_KEYS`), so a model still reads a bare score before deciding to write. What removes the sharpest consequence is on the other side of the call: U4's write-time dedup makes `remember` return `duplicate` with the existing fact id instead of writing twice (`core/write/dedup.ts`, `mcp/tools/write.ts`). This row is the read-side half, and it is not built."}
{"id": "stack.contextual-retrieval", "capability": "Embed-time contextual wrapping of chunks with title and synopsis", "criticality": "critical", "status": "not-yet", "priority": "p1", "unit": "U4", "source": "accuracy-stack", "notes": "The free tier of it ships and this row names both tiers. `core/write/embed.ts:documentEncoding` applies the title wrap at embed time on the write path and on the backfill, and the format is pinned by assertion because changing it re-encodes every brain. The synopsis tier is a *declared deferred phase* (`core/write/phases.ts:ASYNC_PHASES` — `synopsis_wrap`) and costs a model call per chunk, so it is U11's and this stays partial until it lands."}
{"id": "gap.server-instructions", "capability": "The brain ships the instructions that make a connected agent capture to it and consult it — MCP server instructions, tool descriptions, response envelope", "criticality": "critical", "status": "covered", "unit": "U6", "source": "critical-gap-1", "notes": "All three carriers ship. `src/mcp/instructions.ts` supplies dated server `instructions` returned from `initialize` (`mcp/server.ts`); every content-bearing tool description carries the capture-and-consult and untrusted-data clauses from one place (`mcp/tools/index.ts`); the response envelope carries `notice`/`next`/`setup`/`degraded` (`mcp/envelope.ts`). `test/mcp/guards.test.ts` pins the release text, so editing it without cutting a release fails."}
{"id": "gap.read-path-model-spend", "capability": "A settled answer to whether recall gets a query embedding and a rerank call, and who pays for them", "criticality": "critical", "status": "covered", "unit": "U20", "source": "critical-gap-2", "notes": "Settled in code, both halves and the payer. `recall` gets a query embedding: `core/search/read.ts:embedQuery` issues it as the gateway's `embedding` op, so request-path spend is metered against the tenant counter like every other call, and a provider failure becomes a value (`degraded: embedding_unavailable`) rather than an outage. `recall` gets a rerank call as of U12: `core/search/read.ts:scoreWithCrossEncoder` issues the gateway's `rerank` op over the packed candidate set, bounded by `rerank-stage.ts:RERANK_CANDIDATES_DEFAULT`, so request-path rerank spend is metered against the tenant counter like every other call and a provider failure becomes `degraded: rerank_unavailable` rather than an outage. The read path now has two external dependencies, which is what KTD4 says it costs. Who pays: the hosted pooled key unless the tenant stored their own, which still meters but is excluded from hosted COGS (`ai/keys.ts`). **A third degradation joined those two, and closing it exposed a CPU sink nobody had named.** A ~150KB `query` reaches `src/core/search/arms.ts:ftsArm`'s `websearch_to_tsquery` and Postgres raises SQLSTATE `54001` (`check_stack_depth`; `54000`, `value is too big in tsquery`, past about a megabyte). It never surfaced as a 500 — `src/mcp/dispatch.ts` already converts a handler throw into a typed refusal and `src/mcp/server.ts` renders it inside a 200 — but it did surface as `code: 'error'` with 'That call could not be completed', a whole-read refusal on the path whose own prose beside `READ_PATH_SPEND_CEILING` promises a caller feeding a megabyte of 'query' gets 'a degraded answer rather than an invoice'. `ftsArm` converts those two SQLSTATEs — and only those two, so a connection reset or a statement timeout still throws — into an empty arm, and the read reports `query_too_complex` through U6's envelope. **The second half mattered more:** `src/core/search/normalize.ts:longestPhraseRun`, which the title-phrase boost runs per candidate with the *query* as its needle, searched from the needle's own length and was therefore about cubic in what the caller typed — 235ms at 10KB, 1.7s at 20KB, 13s at 40KB, and over eleven minutes of CPU without returning at 150KB. Above the tsquery threshold the throw hid it; below the threshold nothing did, so a 40KB query already bought thirteen seconds of one request path. The inner loop is now bounded by the haystack length, which changes no result — `runIndex` refuses any run longer than the haystack as its first act — and takes the 40KB case under a millisecond. `test/mcp/oversized-query.test.ts` reproduces the SQLSTATE, the degraded envelope, the closed caught set and the bound. **Metered is not capped, and this row says only the first.** Request-path spend accumulates into the tenant counter and is bounded per request by `src/core/search/read.ts`'s own ceiling; no tenant-level ceiling refuses a read. That half is `gap.request-path-tenant-cap`."}
{"id": "gap.answer-quality-measurement", "capability": "An owned fixture corpus scoring answer quality — absolute floors, NamedThingBench-class gates, extraction recall floor", "criticality": "critical", "status": "not-yet", "priority": "p0", "unit": "U7", "source": "critical-gap-3", "notes": "Explicitly separate from wire conformance, which passes green while write-back drops a fact. Two of the three named parts ship: the owned fixture corpus with absolute floors, and the NamedThingBench-class per-question-type gates, both enforced by `bun run eval:blocking`. The extraction-recall floor is still unscored — R6 puts model-extraction recall in the canary tier, whose producing op (`extract`) is a U11 deliverable, so evals/canary.ts carries it as a derived deferral rather than a grade. Not `covered` until that floor is actually scored."}
{"id": "gap.free-tier-line", "capability": "A free tier that is a coherent product rather than a demo — the extraction line redrawn, or a graph_degraded envelope flag stated as honestly as search_degraded", "criticality": "critical", "status": "not-yet", "priority": "p0", "unit": "U11", "source": "critical-gap-4", "notes": "U11 drew the line and made it enforceable: `FREE_TIER_PHASES` **is** `DETERMINISTIC_PHASES` (one list, aliased rather than copied), a free-tier cycle stops there with `dreamt: false` and `stop_reason = 'free_tier'` on the run record, and `test/consolidate/free-tier.test.ts` measures what the free tier still buys on the committed corpus — duplicate facts 17 to 0, missing edges 4 to 0, zero model calls. It also answers the open question above for the cycle: consolidation's free tier calls no model at all, embeddings included, so a free-tier cycle adds no vectors and the write path's own first-import gate (U8) owns that spend. U12 shipped the briefing half: `core/briefing/assemble.ts` returns `coverage: 'cold'` with a `not_included` list naming participant cards, extracted commitments and the synopsis layer for a brain the model tier has never completed over — which is the permanent and correct state of a free-tier brain — and its upgrade prompt reads the deterministic pending-debt count and has nowhere to put a contradiction count, bounded once per threshold crossing or per fortnight. STILL NOT-YET, and now for exactly one reason: the `graph_degraded` envelope flag is U6's and does not exist. `DEGRADED_REASONS` carries no graph entry, so a read whose graph arm found nothing is indistinguishable from one whose graph arm was never asked."}
{"id": "gap.ingestion-substrate", "capability": "Ingestion as a substrate — per-source cadence, idempotent staggered retries, per-item ingest log, per-source locking, resumability, a delivery channel", "criticality": "critical", "status": "not-yet", "priority": "p0", "unit": "U9", "source": "critical-gap-5", "notes": "The staleness surfaces ('nothing from this source in 23 days', 'what is not indexed yet') both require the per-item log, not just the connector."}
{"id": "gap.chat-export-import", "capability": "Bulk import of a user-uploaded chat export — the one conversation path a stateless MCP server can have", "criticality": "critical", "status": "not-yet", "priority": "p0", "unit": "U8", "source": "critical-gap-5", "notes": "An MCP server sees tool calls, not transcripts. Without this, conversation content has no path in at all."}
{"id": "gap.auth-enforcement-point", "capability": "One operation declaration behind every surface, with the fence check below the handlers and a CI guard failing on any handler-local auth check", "criticality": "critical", "status": "covered", "unit": "U6", "source": "critical-gap-6", "notes": "`src/mcp/dispatch.ts` is the single entry point and the order is fixed: credential, then the fence derived from that credential, then the tenant connection opened by an identity that reaches no other tenant, then the handler — which receives the grant as a value and has no way to widen it. The tool table in `mcp/tools/index.ts` is the one operation declaration behind both endpoints. The CI guard is `test/mcp/guards.test.ts`, which scans `src/mcp/tools/` and fails a handler that resolves a credential, takes a grant/origin/tenant from its arguments, or writes SQL; `bun test` runs on every PR."}
{"id": "gap.context-injection-gate", "capability": "A zero-tolerance context-injection gate covering derived-row inheritance, graph traversal across contexts, and post-severance reads", "criticality": "critical", "status": "not-yet", "priority": "p0", "unit": "U16", "source": "critical-gap-6", "notes": "Three legs. Two land at U18 and the third is now fenced; the row stays open on a fourth thing the audit did not name, stated below rather than left to be inferred. **Derived-row inheritance: covered.** A mixed-origin fact is refused to a work-scoped grant on the SUBSET rule (`src/core/search/fence.ts`), and the union it is fenced on is enforced on the write side by the schema's own origin-union triggers rather than by the cycle remembering to record it. `src/worker/consolidate/materialize.ts` now refuses to file a synthesis whose inputs span more than one origin at all, because `page.origin_context` is a scalar and there is no honest one to choose — it used to take `origins[0]` off a sorted union, which put the summary in whichever half of the brain sorted first. **Post-severance reads: covered.** `src/core/lifecycle/severance.ts` tombstones the exactly-origin rows and `test/core/lifecycle/severance.test.ts` asserts through the real surface that no severed row comes back, with a companion assertion that the surviving halves still do. **Graph traversal: now covered for vocabulary as well as content.** `src/mcp/reads.ts` resolves a shared entity on the intersect rule and fences its fact hydration on the subset rule. The alias list, which used to be returned unfenced, is fenced on the same subset rule: rung 11 (`src/schema/migrations/v11-alias-origin.sql`) gives `entity_alias` an immutable `origin_contexts`, `src/core/write/links.ts` and `src/worker/consolidate/deterministic.ts` stamp the origins of the write that planted the spelling, and the read coalesces an unstamped legacy row to its entity's own union — the fail-closed reading, since expand-only forbids a backfill and immutability forbids one later. `EntityCard.origins` still returns the entity's full origin union, so a work grant still learns which contexts a shared name appears in; that is a label rather than a statement, and it is a bound this row asserts rather than hides. **The producer the grants did not have is built, and what keeps this row open has moved.** `ConnectorState.contextClass` (`src/ingest/cursor.ts`) records which context class a connection belongs to, and `src/ingest/pipedream/pull.ts:originContextFor` turns it into the origin every page, chunk and fact that source writes — a connection recorded as `work` files at `work:gmail` rather than at `pipedream:gmail`. `test/ingest/pipedream/context-origin.test.ts` proves the round trip through the real `expandGrant` and the real `fenceScalar` (a `work:*` grant admits the mailbox, a `personal:*` grant does not) and asserts the derivations carry the origin too, since a producer that stamped the page and left its chunks behind would be a work grant that finds a document and none of what was drawn from it. **No source name is hardcoded to a class** — which mailbox is work is the user's answer, so the class is a recorded value validated through `grant-scope.ts:classOf` rather than through a second grammar, and `test/ledger/context-grant-producer.test.ts` now pins a producer set of two: the hardcoded `personal:agent` default write origin, and this one, which has no literal to grep for by design. A malformed class yields no connector state at all rather than a null one, because dropping it would file a work connector's mail where the user's own work grant cannot see it. Null keeps `pipedream:<source>`, which is the safe direction rather than the timid one: `origin` is immutable by trigger and U4's replacement lookup keys on `(external_ref, origin_context)`, so re-originating a live connection would not move its pages — it would strand every one at the old origin, out of reach of a tombstone sweep that scopes by origin, and write a second live page for each on the next poll. A class change is therefore a disconnect-and-reconnect, stated beside the field. **What keeps this row `not-yet`: the seam is built and no connect flow can reach it.** Nothing in `src/` writes a `ConnectorState` at all — `connectSource` and `createObjectConnectorStore` have no caller outside tests, and `/api/connectors` mints a claim URL and stops — so U15 still owes the two halves that make this a capability rather than a seam: a consent surface that asks whether an account being connected is work or personal, and a claim-redemption path that records the answer on the state. Until both land, a user cannot obtain a work-origin connector and a `work:*` grant on a real brain still expands to `['work:agent']`. `test/mcp/context-grants.test.ts` still plants its `work:mail` rows in SQL and its header still says so, so a green run there is still not read as the capability. **Two statements this change made stale are named rather than swept:** the 'WHAT A CONTEXT GRANT CAN CURRENTLY READ' section of `src/mcp/grant-scope.ts` and the header of `test/mcp/context-grants.test.ts` both say that no production write path produces a context-class origin for a connector. That is now false of a connection carrying a class, and correcting those two files belongs to whoever owns them — recorded here so the drift is attributable rather than silent."}
{"id": "gap.write-path", "capability": "A defined write path: the sync/async split, a chunking spec, the hard 6000-character safety cap, and CJK-aware sizing", "criticality": "critical", "status": "covered", "unit": "U4", "source": "critical-gap-7", "notes": "All four named parts. The sync/async split is data rather than prose — `core/write/phases.ts` declares it and `PhaseRecorder` refuses a run that skipped a synchronous phase or performed a deferred one inline, so the split cannot degrade silently in either direction. The chunker is heading/paragraph-aware with overlap, carries a hard `MAX_CHUNK_CHARS = 6_000` cap that splits over-cap content rather than dropping it, and switches to per-code-point sizing above a 30% CJK ratio (`core/write/chunker.ts`)."}
{"id": "gap.provision-time-decisions", "capability": "The per-tenant decisions taken before the first project is provisioned: FTS language, image embedding column, embedding dimensions, taxonomy version", "criticality": "critical", "status": "covered", "unit": "U3", "source": "critical-gap-8", "notes": "All four are taken before the first write is accepted. FTS language is a NOT NULL control-plane column with no default, chosen at provision, substituted into the generated tsvector columns and verified after apply (`control/provision.ts` → `schema/apply.ts` → `schema/fts-language.ts`). Taxonomy version is `tenant_setting.taxonomy_version`, read back by the write path rather than defaulted, and stamped per row so a re-classify is a job with a backlog. Embedding dimensions are 1536 in the DDL and in `schema/vector-index.ts`. The image column is reserved as `attachment.image_embedding vector(1152)`."}
{"id": "gap.fleet-migration", "capability": "Migrating ten thousand suspended databases — warm-compute-opportunistic fan-out, per-tenant schema version, partial-failure tolerance at request time, rollback", "criticality": "critical", "status": "not-yet", "priority": "p0", "unit": "U3", "source": "critical-gap-8", "notes": "Wired, and still short of the row's last clause. `control/migrate.ts` has the per-tenant version ladder banked tenant-first, the advisory-lock-per-database wake path, a bounded failure-isolated sweep with a per-tenant deadline, typed behind/ahead refusals with one rung of lookahead, and the expand/contract plus rollout-ordering checks. Both legs now have callers: `mcp/dispatch.ts` asserts a servable schema before the fence is derived or any handler runs — the version rides the connection entry `mcp/tenant-db.ts` already caches, so no warm request pays a round trip, and the refusal path re-reads once so a tenant the sweep has just migrated is served on the next call rather than at TTL expiry — and `worker/scheduler.ts:runSchedulerTick` sweeps every tick, bounded by `schemaSweepLimit`, ahead of the enqueue, through `control/schema-sweep.ts` ports that list behind tenants warmest-first (`last_activity DESC`, the warm-compute-opportunistic half) and bank the version forward-only onto the control-plane row. Not covered because the row also names **rollback**, which does not exist and is currently answered by a different mechanism instead: rungs are expand-only and `findRolloutViolations` refuses to un-ship one. Per-tenant staged rollout is its own row (gap.per-tenant-feature-flags)."}
{"id": "gap.per-tenant-feature-flags", "capability": "Per-tenant feature flags as the staged-rollout half of fleet migration", "criticality": "critical", "status": "not-yet", "priority": "p1", "unit": "U19", "source": "critical-gap-8", "notes": "Half of it ships and half does not, and the row stays `not-yet` for the half that does not. SHIPPED: `src/control/flags.ts` over a `control.tenant_flag` table in `src/control/schema.sql` — one row per (tenant, flag), a three-value stage (`off`/`canary`/`on`), fail-closed so an unset flag is off, a committed flag registry so a typo cannot stage for nobody and look deliberate, and a cohort query. It has one real consumer: U19's change channel (`src/upstream/change-channel.ts`), which is how it is exercised end to end in `test/upstream/change-channel.test.ts`. STILL ABSENT, which is what this row is about: nothing in the write path reads a flag, so a new chunker or a revised extractor prompt still ships all-at-once. The mechanism exists; the staged rollout it was asked for does not. Flip this when a fleet behaviour is actually gated on a stage."}
{"id": "gap.unverified-model-output", "capability": "A verification tier for model-derived knowledge — confidence-gated mutation, a review queue, quarantine on novel claims, and an unverified flag the consuming model can weigh", "criticality": "critical", "status": "not-yet", "priority": "p0", "unit": "U11", "source": "critical-gap-9", "notes": "Two of the four parts are built. Confidence-gated mutation is `materialize.ts:gateFor` — >=0.8 applies, 0.5-0.8 queues, <0.5 is counted and dropped — and the review queue is a real table whose closing channel is constrained in the schema, not in code: `review_queue_closed_by_is_out_of_band` admits `user_out_of_band` and `internal` and refuses `agent_mcp`, because R12a says a restatement over MCP clears nothing. Every model-derived row also carries `trust_level`, and `fact_model_rows_carry_a_trust_level` refuses a model-derived fact without one. Still not-yet for the other two: there is no quarantine on novel claims, and the trust level reaches no consuming model — the envelope that would let one weigh it is U6's."}
{"id": "gap.report-only-drift", "capability": "Contradiction handling that reports rather than resolves — never silently overwriting an opinion the user recorded", "criticality": "critical", "status": "covered", "priority": "p0", "unit": "U11", "source": "critical-gap-9", "notes": "U11's contradiction phase detects and reports; there is no apply branch in it at any confidence. `runContradictionPhase` writes a `contradiction_report` row and nothing else — the two facts it names are untouched, which `test/consolidate/guards.test.ts` asserts by snapshotting the user-stated row before and comparing it after, at a model confidence of 0.95 (above the apply gate, so the test is about the phase rather than about the gate). The gate still runs, on whether the report is worth making: below 0.5 the model is guessing, and a fabricated conflict is worse than a missed one when the count is what R8's upgrade prompt shows the user. Mutating the phase to supersede on a high-confidence conflict turns that test red."}
{"id": "gap.injection-sanitisation", "capability": "Prompt-injection sanitisation on the way in and out, plus an anti-loop guard on the cycle that reads the store it writes", "criticality": "critical", "status": "not-yet", "priority": "p0", "unit": "U11", "source": "critical-gap-9", "notes": "The anti-loop guard is done and is the load-bearing half: `selectExtractionCandidates` admits `page.derivation = 'ingested'` and nothing else, so a canonical summary written by cycle N is never evidence for cycle N+1, and the marker is a NOT NULL column with a CHECK rather than a convention. Inbound, every model prompt wraps external-origin content through `mcp/demarcation.ts` and says in the system half that the region is data — keyed on the origin union (R15), not on `source_type`, because the surface is credential-derived and the type is not. Still not-yet because that is demarcation rather than sanitisation, exactly as this note said of the outbound side: nothing strips or rewrites hostile content, and nothing scans a model's own output before it is stored."}
{"id": "gap.data-lifecycle", "capability": "Page versioning and revert, soft-delete with TTL and restore, hard purge, and blast-radius preview before any destructive op including severance", "criticality": "critical", "status": "not-yet", "unit": "U17", "source": "critical-gap-10", "notes": "Four legs, three of them wired and one not — which is why this row is `not-yet` rather than covered. **Wired.** U6 shipped the soft-delete leg ahead of this unit because `forget` went live in Phase 1: tombstone-only with a 72h recoverable window, a chosen cascade, restore-by-instant and an ordered hard purge (`src/mcp/tombstone.ts`), which reaches `commitment` and `attachment` by their own `deleted_at` because neither has a parent to cascade from. **The blast-radius preview has two columns** (`src/core/lifecycle/blast-radius.ts`): severing an origin *removes* rows whose origins are exactly it and *recomputes* every mixed-origin fact, entity, card, commitment and edge, because a derived row carries the union of its inputs' origins (R15) and losing one input leaves it wrong rather than gone. `test/core/lifecycle/blast-radius.test.ts` opens with a test asserting the fixture actually contains mixed rows, because a severance preview over a brain with none passes trivially, and closes by running `forgetRecord` itself over the same fixture so the mirror is compared rather than trusted. **U18 gave `previewSeverance` its executor and the web app gave that executor its route.** `src/core/lifecycle/severance.ts:severOrigin` re-runs the preview *inside* the executing transaction — the number the dialog rendered is not the number that happened once a connector poll lands between them — tombstones the exactly-origin rows on `forget`'s existing 72h clock rather than hard-deleting them, and appends one row to rung 10's `severance` table carrying the two count objects and the surviving origins. The recompute worklist is DERIVED from that row plus each row's own immutable origins rather than stored as a per-row flag, so it cannot go stale or disagree between tables. Confirmation is an echo of the origin string, not a boolean, and severance is deliberately not on `tools/call` at all. `src/web/serve.ts` now builds the `SeverancePort` that `src/web/app.ts` declares, so `/api/severance` and `/api/severance/preview` reach a real brain instead of answering `501 unavailable` in every deployment; `test/fleet/severance.test.ts` drives the spawned entrypoint and reads the effects out of the tenant's own database rather than out of the response. **Not wired: page versioning and revert, and it is the reason this row is not covered.** `src/core/lifecycle/versions.ts` is complete and proven — `page_version` (rung 9, `src/schema/migrations/v9-lifecycle.sql`) is an explicit snapshot keyed on the *document* rather than the page row, with `ON DELETE SET NULL` so the TTL purge cannot take the history with it; a snapshot that cannot be verified against `page.content_sha256` is refused rather than written; revert re-ingests through U4 while banking the state it replaced — and it has **no caller anywhere in `src/`**. Nothing sweeps a superseded predecessor into a version before the purge takes it, no surface lists a document's history, and nothing can revert to one. Every caller is a test, so what is proven is that the module works, not that a user has a version history. `test/ledger/coverage-claims.test.ts` resolves the importer set from the source tree on every run, so this row stops being held down the day a production caller lands, with no edit to the rule. **Two smaller halves stay open:** `previewForget` has no caller — `forget` does not preview before it retracts — and re-derivation of the recomputed rows is U11's cycle, which has no consumer of the worklist yet. **A cross-origin defect in that unwired module was closed before it could acquire a caller.** `external_ref` carries no cross-origin uniqueness — `src/core/write/write-path.ts:livePageByRef` narrowed its own replacement lookup to `(external_ref, origin_context)` for exactly that reason, because a shared calendar event is pulled by a work connector and a personal one — and three statements in `src/core/lifecycle/versions.ts` still read \"the row with this ref\" as \"this document\": the superseded sweep's join (which made a page retracted by `forget` look like a predecessor whenever another origin held the same ref, banking it verbatim into a table the 72h purge cannot reach), the capture's head lookup (which answered `unchanged` to a document with no snapshot at all, because the *other* mailbox's byte-identical copy had one), and the revert's live-page lookup (`ORDER BY page_id DESC` over every origin, so a work-scoped revert banked a `pre_revert` snapshot of the personal page). All three are fenced now and `test/core/lifecycle/versions-origin-fence.test.ts` builds each violating case. **The half that was left open — the shared sequence — is closed, and the fix is NOT the rung this row previously sketched.** `doc_key` now folds the origin in: `src/core/export/reconstruct.ts:docKeyFor` returns `<origin>|<external_ref>` (or `<origin>|page:<id>`), with `parseDocKey` as its one inverse so the two directions cannot disagree, and `|` is a character the origin grammar cannot produce, which makes the split a split rather than a guess. Each origin's copy therefore allocates from its own sequence, contiguous from 1 — which is what the rung's own DDL says a version number is and what it could not be while the key was the bare ref. The capture's two queries collapse into one (the head of a folded key answers both 'has this changed' and 'what number is next'), `revertPage` resolves its live page from the parsed ref, and `subject-erasure.ts` carries the page's origin on `SubjectMatch` so it can name the keys a snapshot of that page would sit under. **The renumbering this row asked for cannot be shipped, and the reason is the ladder's own rule.** `UPDATE page_version SET doc_key = …` is refused by `src/control/migrate.ts:findExpandContractViolations` — verified by running it — because the previous fleet version still reads those rows by the bare key, and that function's header says there is no waiver list on purpose: a genuinely contracting change needs a contract rung gated on every instance having been replaced, and no such mechanism exists. Re-numbering under a new unique index is refused by the same scanner for the same reason (uniqueness added to a pre-existing table). So there is no rung at all here: the fold is a change in what the code *writes*, the schema is untouched, and rows banked before it keep their bare keys and keep resolving. **The residue, named rather than rounded up:** a brain holding pre-fold snapshots has that document's history in two sequences, and `subject-erasure.ts:docKeysOf` therefore names both the folded key and the legacy bare one — dropping the legacy arm would leave a pre-fold copy of an erased correspondent's mail in the one table the 72h purge cannot reach. In production that residue is empty, because this module still has no caller; doing the fold before one lands is what keeps it empty. `test/core/lifecycle/versions-origin-fence.test.ts` asserts the closed property (both origins number from 1, and a key from one origin is refused `scope_denied` to a grant on the other), and `test/core/lifecycle/subject-erasure.test.ts` adds the case the key arm exists for: a snapshot of her page whose own text does not name her — the ordinary shape, since a history records a document before it said what it says now — which the text arm cannot reach and which the document-key arm takes with the page. **The restore leg was covering four of seven table classes and now covers all seven.** `forgetRecord` tombstones `page`, `chunk`, `fact` and `entity`; `src/core/lifecycle/severance.ts:severOrigin` tombstones `entity_card` and `commitment` at the same instant, and `src/core/lifecycle/subject-erasure.ts:eraseSubject` a seventh, `attachment`. `restoreForgotten` knew about the first four, so a user who disconnected the wrong account or erased the wrong correspondent pressed undo, got `ok: true`, and got a brain with its entity cards, its commitments and its attachments still deleted — which both of those modules' headers explicitly rely on not happening. The correspondence is structural now rather than remembered: `TOMBSTONED_TABLES` in `src/mcp/tombstone.ts` is one ordered list that both the restore and the purge walk, and `test/core/lifecycle/restore-coverage.test.ts` reads `information_schema` and refuses any table carrying `deleted_at` that appears in neither that list nor the reasoned exclusion list — `entity_edge`, whose `deleted_at` is a reconciliation retraction rather than a user's. The same file pins the one collision a restore can meet: `entity_card_one_live_per_entity` is UNIQUE over live cards, so a card written by a later cycle blocks the un-delete of the one it replaced; that row stays deleted and the outcome says `supersededCards` rather than raising a 23505 that would abort every other table's restore. **Both residue counters were then found to be reporting the wrong number, and it is fixed.** `supersededCards` and `supersededAliases` were each computed as `rows still carrying the instant` MINUS `rows just restored` — but that count runs AFTER the restore has already cleared or deleted the rows it took, so the remainder IS the residue and the subtraction removed it a second time. Over two rows at one instant: both blocked reported 2 (right by accident, and the only shape the existing suites built), one blocked reported 0, none blocked reported -2. A caller told `supersededAliases: 0` had in fact had a spelling not come back, and one rendering a `N could not be restored` line was handed a negative count of rows. Both counters now read the remainder and report it; `test/core/lifecycle/restore-residue-count.test.ts` builds the residue that is neither empty nor total, checks each counter against the database rather than against itself, and opens with a test asserting the fixture is a mix — because over a total residue it would pass against the defect. **The removal class was then audited against the preview column by column, and the two columns `tombstoneExactOrigin` never touched turned out to be different findings.** `attachment` was a real residue and is fixed: it is scalar-origin, carries its own `deleted_at`, is already swept by `purgeExpiredTombstones`, and was simply absent from the executor — and no cascade covered it, because `attachment.page_id` is nullable and carries no origin-union constraint against its page, so a work attachment can hang off nothing at all or off a page that survives. It is now tombstoned on the severance's own instant, which puts it inside the same undo and the same purge as everything else. `entity_edge` was NOT a residue and is deliberately left alone: `entity_edge_origin_union` (rung 2) refuses any edge that does not carry the origins of BOTH entities it connects, so `edge <= {severed}` forces both endpoints to be exactly severed — both tombstoned by the executor's `entities` statement, both hard-deleted by the purge, and the edge leaves with them through ON DELETE CASCADE, with no live endpoint for `src/core/search/arms.ts:graphArm` to seed a neighbourhood from in the meantime. That is the same structural argument `test/core/lifecycle/severance-purge.test.ts` already makes for `entity_card`, and writing a `deleted_at` there instead would have been actively wrong: `src/mcp/tombstone.ts:DELETED_AT_IS_NOT_A_TOMBSTONE` reserves that column for a reconciliation retraction and keeps the table out of `TOMBSTONED_TABLES`, so severance's tombstone would have been one no sweep reaches and one an undo could resurrect as live. `test/core/lifecycle/severance-removal-class.test.ts` asserts both halves — the database REFUSES the mixed-endpoint shape that would strand an edge, and over the shape that IS reachable nothing work-origin survives the purge. **That half of the column is now closed, and a second disagreement was found beside it.** `previewSeverance`'s `attachment` and `entity_edge` arms carried no `deleted_at IS NULL` filter where its `page`, `chunk` and `fact` arms did, so the preview counted every attachment a prior `forget` already held and every edge a consolidation cycle had already retired as rows this severance was about to take. The executor keeps the filter regardless — re-stamping a row another retraction took would move it onto this instant and detach it from that call's undo — so the over-count was the preview's alone, and since the preview is re-run INSIDE the executing transaction and written into rung 10's `severance` row, it was also the number the audit trail kept. Both arms filter now. The second disagreement ran the other way: `RemovalCounts` had no `aliases` column at all, so the preview never mentioned the take severance performs by MOVING rows into `severed_alias` — the one row class with no tombstone behind it — while the receipt had carried `archived.aliases` since that shipped. The column exists now and mirrors `archiveExactOriginAliases` statement for statement (the join, the coalesce that reads an unstamped legacy row as its entity's own union, and no liveness filter on the entity, because the executor has none either), so preview, receipt and audit jsonb describe one event. It is zero in the `recomputed` column deliberately: an alias asserts nothing, so it cannot be left wrong by losing an input, and `recomputeWorklist` has no producer for it — counting mixed aliases there would report a recompute cost nobody pays. `test/core/lifecycle/severance-preview-agreement.test.ts` builds the violating fixture (an attachment a prior forget holds, a retired edge on both sides of the origin split, three aliases of which two are severance's first class) and compares the three surfaces against each other. **Severance left `entity_alias` behind and the surviving rows were still served; that is closed, by a move rather than a flag.** Rung 11 gave `entity_alias` its own `origin_contexts` so an alias can be narrower than its entity — an alias is the normalized surface form `resolveOrCreateEntity` took from ingested text, which is a spelling an outside sender chose in one mailbox. That licence is exactly what made the residue reachable and unique to this table: every OTHER derived table carries a covering constraint (`entity_card_origin_union`, `entity_edge_origin_union`, `commitment_origin_union`) that forces an exact-origin row to have an exactly-severed parent, so severance takes the parent and the cascade takes the row. `entity_alias` has none by design, so a work-only alias on a MIXED entity was severance's first class with nothing to take it: `tombstoneExactOrigin` did not write to the table, `entity_alias` has no `deleted_at` column at all, and its only route out is the entity's ON DELETE CASCADE — which never fires, because the entity is mixed and survives by design. **The fix is rung 12, `src/schema/migrations/v12-severed-alias.sql`:** `severOrigin` MOVES every alias whose `coalesce(a.origin_contexts, e.origin_contexts)` is a subset of the severed singleton — the read fence's own expression, read backwards, so what leaves is exactly the set of spellings only a grant holding the severed origin could ever have been shown — into a `severed_alias` holding pen carrying the severance instant. A move rather than a `deleted_at` for two reasons that are facts about this repo rather than preferences: nine sites read aliases (`reads.ts` x3, `search/read.ts` x2, `search/arms.ts`, `briefing/assemble.ts`, `write/links.ts` x2, `subject-erasure.ts`) and a tombstone is only honoured by the sites that remember its predicate, whereas a row that is not in `entity_alias` cannot be returned by a query against `entity_alias`; and `entity_alias_is_unique_per_entity` is a TOTAL unique constraint, so a tombstoned alias would hold its own spelling's slot against re-creation while making that index partial is a contracting change the rung discipline forbids. An archived row holds nothing, which `test/core/lifecycle/severance-alias-residue.test.ts` measures by writing the same spelling back while the row is away. Severance stays no more final than `forget`: `restoreForgotten` walks `ARCHIVED_TABLES` beside `TOMBSTONED_TABLES` and moves the row back verbatim by the same instant, reporting `supersededAliases` for a spelling that is live again rather than raising a 23505 that would abort every other table's undo; `purgeExpiredTombstones` empties the archive on the same 72h cutoff. That file's second describe used to assert the broken behaviour on purpose and now asserts the closure. **`src/mcp/reads.ts:brainOrigins` still resolves a severed origin, and that is reported rather than fixed, with the measurement behind it.** Origins are immutable by trigger and the surviving mixed rows carry the severed one, so the census reports `work:mail` after the severance and after the purge. Subtracting it is the only way to stop that, and it is the same knob as 'may a whole-brain grant read a mixed row': `fence.ts:fenceRow` is a SUBSET rule, so the instant `work:mail` leaves the grant every mixed fact, mixed card and shared alias stops being readable — reversing `severance.ts`'s 'rows whose origins include it and others — these stay', which `test/core/lifecycle/severance.test.ts` pins as 'the surviving halves are still there — severance is not a purge of the brain'. There is no setting that does both. What makes the origin harmless to resolve is that nothing in the removal class survives to be reached through it, which is what the two fixes above establish; the residue test asserts both halves — the census still contains the severed origin, and every live row carrying it is a mixed one — and demonstrates the cost of the alternative by narrowing the grant and watching the SHARED alias disappear. The residual, named: a connector that keeps WRITING under a severed origin re-enters the census through the `page` arm on its next poll, which is `grantsEmptiedBy`'s revocation leg rather than the census's, because a census that hid live rows would be lying about what the brain holds.", "priority": "p0"}
{"id": "gap.erasure-path", "capability": "An erasure path spanning Neon, R2 and the control plane, plus a subject-scoped erasure answer for a non-user correspondent", "criticality": "critical", "status": "not-yet", "priority": "p0", "unit": "U17", "source": "critical-gap-10", "notes": "Built and proven against fakes; **not flipped, and the missing half is named.** `src/core/lifecycle/erasure.ts` runs the five legs the roadmap names — connector external-user deletion (this is `PipedreamClient.deleteExternalUser`'s first caller anywhere in `src/`), the tenant's stored BYOK provider key, the R2 prefix, the Neon project, and the control-plane row with the secret-store entries it pointed at — credentials first and data last, with the control-plane row surviving any failed leg because it is the only record of what to retry. `test/core/lifecycle/account-erasure.test.ts` seeds every store, asserts each is non-empty, and then asserts *absence* rather than that a call happened. The receipt carries `tokensRevoked: 'unverified'` verbatim from the vendor. `src/core/lifecycle/subject-erasure.ts` answers the second axis: keyed on a correspondent identifier, spanning the entity graph and the identifier as text, reporting what survives but must be re-derived, and writing an `erased_subject` tombstone that stores a digest rather than the address. **Two halves are still open.** (1) No leg has been executed against a live vendor — Neon delete, R2 delete and the Pipedream DELETE are exercised against fakes only, so 'no queryable trace' is proven in this repo and not in production. (2) **The `erased_subject` reader is now wired, and that half is closed.** `src/ingest/pipedream/pull.ts` consults it through `src/ingest/erased-subjects.ts` before the estimate — ahead of the meter for the same reason the junk gate is, since an item that will never be written must not price the approval — and a suppressed item is a named disposition on the ingest log (`suppressed`, `items_seen = 1`, `items_written = 0`) rather than a silence, because a poll that dropped items without saying so is indistinguishable from a provider that stopped offering them. It does not hold the cursor: a suppression is a stable fact about the item, like an empty document, and holding would wedge the source forever on a message that is never going to become ingestable. `test/ingest/pipedream/erased-subject.test.ts` drives the **real** `eraseSubject` and then re-offers the same item from the same source, so what is proven is that the digest the pull hashes is the digest the erasure wrote — a planted tombstone row would only have proven the pull can read a digest it was handed. **Three bounds are named rather than rounded up.** The tombstone stores a sha256 and nothing else, deliberately, so a pull can only ask about identifiers it can name: addresses in the correspondent headers and in the item's own title and body (which is what covers Calendar, whose adapter composes attendee and organizer addresses into the body), plus display names attached to an address. A message naming her in prose with no address anywhere is not suppressed, and the last test in that file pins it as an observed fact — closing it would need a plaintext list of erased names, which is the failure wearing the fix's clothes. Objects are not consulted either: a `PulledMedia` is bytes and a content type, and its only text arrives from U11's transcribe phase long after this decision. And the **manual** import paths (`src/ingest/import/run.ts`, folder and chat-export) do not consult it — the property U15 flagged is the cadence, and a re-import is a controller acting deliberately rather than a machine undoing a receipt on a five-minute timer, but a user who re-imports an mbox after an erasure gets her mail back and that is stated here rather than left to be discovered. **An adversarial pass over Phase 4-5 moved the subject path, and did not move this row.** Of the two open halves that pass named at the time, the re-ingestion one is now closed (above) and the live-vendor one is unchanged: no leg has run against a live vendor. What changed is that the sweep now matches a name rather than a substring — every surface form becomes a regex with word-boundary assertions, so a two-character inferred alias like `al` no longer soft-deletes another correspondent's commitment — and the preview names every row it will take (`SubjectErasurePreview.rows`) rather than reporting a count nobody can inspect, which is what the module's stated mitigation (the controller reads the preview before instructing) needs to be true. Three residue classes the 72h purge cannot reach are now swept by the erasure itself: `page_version` snapshots matched on their own text (the ordinary case is a document edited after the snapshot was banked, where no live page names her), `review_queue` proposals in every state, and `attachment` rows plus the stored object under the key the row recorded. A fourth was found while proving the third and fixed one table over: `purgeExpiredTombstones` reached neither `commitment` nor `attachment`, so a commitment with a NULL page and a NULL fact was soft-deleted forever. **Two limits stay named rather than closed.** `ErasableObjectStore` still has no production implementation, so the account path's object leg is proven against a fake only — it now re-lists after deleting and reports `failed` when objects remain, which is what constrains the implementer. And revoking a BYOK provider key cannot reach another fleet process's cache: the bound is published as `TenantProviderKeyStore.cacheWindowMs` and carried on the receipt as `providerKeyCacheWindowSeconds`, the way the vendor's `tokensRevoked: 'unverified'` is carried, because eliminating it needs a shared revocation channel this system does not have. **A third open half, named now that somebody looked for it: neither erasure path had a caller in `src/`. Half of that is now closed — the subject path — and the account path is not.** `eraseAccount` is still imported by its own test and by nothing else: no route, no job handler and no operator command reaches it. That is not a small wiring edit deferred out of laziness, and it stays recorded rather than closed, because leg 3 needs an `ErasableObjectStore` implementation that does not exist in `src/`, leg 4 needs the Neon vendor credential, and leg 1 needs Pipedream's; a route built ahead of those would answer `complete: false` on every run for reasons no operator could act on, which is a worse artifact than an honest gap. **The subject path had no such dependency and its absence was the wiring edit.** R12's third property is that erasure be invocable by the controlling user OUT OF BAND, and `src/web/app.ts` already declared a `SubjectErasurePort` and routed `/api/subject-erasure` and its preview through it — `src/web/serve.ts` supplied a `severance` port and no `subjectErasure`, so both routes answered `501 unavailable` in every deployment and a correspondent's request could only be executed by running a test harness against a brain. The port is built now, out of the same tenant resolver the severance port uses; it re-checks the confirmation echo before opening a connection, because `eraseSubject` takes none of its own and there is therefore no third check underneath; and it passes NO object store and no raw-key deriver, so every raw payload and stored attachment is reported UNREACHABLE on the receipt rather than rounded down — the honest shape of the bound this row keeps open, carried to the data subject rather than hidden from them. `test/fleet/subject-erasure.test.ts` drives the spawned entrypoint and reads the effects out of the tenant's own database (her page, chunk, fact and entity tombstoned, the `review_queue` proposal hard-deleted, the `erased_subject` row carrying `erased_by='app'`, and the other correspondent's mail untouched), because an in-memory test with a fake port proves a handler calls a port and cannot prove a deployment has one. This row stays `not-yet` on the two halves that have not moved: no leg has run against a live vendor, and the account path still has no caller."}
{"id": "gap.first-import-gate", "capability": "A delta-aware first-import gate with a bounded first window offered as a product moment, auto-deferring to a capped background job", "criticality": "critical", "status": "not-yet", "priority": "p0", "unit": "U8", "source": "critical-gap-11", "notes": "First connect is tens of thousands of items embedded in one burst for a flat-priced user. Belongs on the chat-export/folder path too, not only on connector pulls."}
{"id": "gap.rolling-spend-cap", "capability": "A rolling per-tenant spend counter on the control-plane row, and a hard fail on any unpriced model", "criticality": "critical", "status": "covered", "unit": "U20", "source": "critical-gap-11", "notes": "src/ai/gateway.ts meters every call against control.tenant.spend_micro_usd, rolling the window in the same UPDATE; an unpriced model hard-fails when a cap is active and records price: unknown when none is. The counter is not a gate: nothing in `src/ai/gateway.ts` can refuse a call on the strength of it, and the two readers that turn the column into a ceiling sit on the ingest and consolidation paths. The tenant-level cap this row's id could be read as claiming is `gap.request-path-tenant-cap`, and it is not-yet."}
{"id": "gap.request-path-tenant-cap", "capability": "A tenant-level ceiling on request-path model spend — one that refuses a call rather than recording it, on the path `recall` and `remember` take", "criticality": "important", "status": "not-yet", "priority": "p1", "unit": "U20", "source": "u20-spend-controls", "notes": "Established absent, not assumed. Four readings, each checked: `src/ai/gateway.ts`'s `SpendMeter` accumulates `control.tenant.spend_micro_usd` after the provider has answered and returns nothing a caller could act on, so it is a ledger and not a gate; the two readers that do enforce a tenant ceiling — `src/ingest/first-import.ts`'s `readHeadroom` and `src/control/tier.ts`'s `consolidationTierOf`, both of which subtract the window's spend from the cap the way `src/control/schema.sql` says to — sit on the ingest and consolidation paths and are never consulted from a request; and `src/mcp/rate-limit.ts` says in its own header that it bounds compute rather than model spend, per Worker isolate rather than globally. What exists on the request path is a *per-request* ceiling in each of the two places that spends: `src/core/search/read.ts` (`READ_PATH_SPEND_CEILING`) and `src/mcp/tools/write.ts` (`REMEMBER_SPEND_CEILING`), each minted per call and each now killed by mutation in `test/core/search/read.test.ts` and `test/mcp/oversized-statement.test.ts`. So one request cannot run away and a tenant can: at the shipped defaults a single grant's 60 requests a minute against a 4,000 micro-USD read ceiling is roughly a quarter of a dollar a minute, metered accurately and refused by nothing. The absence is stated in `src/ai/gateway.ts`'s header — the module a previous comment credited with the cap — and pinned by a tripwire case in `test/core/search/read.test.ts` that asserts ten unrefused reads spending past several ceilings, so the day the layer lands the test and the paragraph are deleted together. WHAT WOULD FLIP THIS ROW: an atomic conditional reservation against the control-plane row — check and hold in one UPDATE, released or settled after the call — rather than a headroom read before `reserve`, which is the check-then-act shape the gateway's own ordering rule 1 refuses because it divides a cap by the concurrency. It costs two control-plane writes per model call on a path with a p99 promise, and it needs a decided fail-open/fail-closed posture for a control plane that cannot be reached, which is why it is a unit rather than a patch."}
{"id": "gap.time-ranged-read", "capability": "Some temporal read on the frozen tool surface — 'what happened last Tuesday', 'what changed since I last looked', 'when did I last see Alice'", "criticality": "critical", "status": "covered", "unit": "U6", "source": "open-question-4", "notes": "The temporal read is `briefing(since, until, focus)` — advertised on both endpoints (`mcp/tools/index.ts`), handled at `mcp/tools/read.ts:briefing` over `core/briefing/assemble.ts` as of U12 (the pre-U11 `mcp/reads.ts:briefingBundle` it replaced is gone), which windows meetings, the delta and the stale list on `created_at`/`stale_at` and defaults to the last 24 hours. The delta half is windowed on a per-caller cursor rather than on `since`, so 'what changed since I last looked' is answered per credential. `recall` deliberately declares no `since`/`until`: filtering after ranking is hazard H3's post-filter recall collapse, so the temporal read is a tool rather than a parameter, and the freeze is not the obstacle this row assumed."}
{"id": "gap.user-excluded-content", "capability": "User-designated content excluded from ordinary recall — a journal, therapy notes, medical records", "criticality": "critical", "status": "not-yet", "priority": "p0", "unit": "U6", "source": "out-of-scope:multi-source-axis", "notes": "Unchanged and still unowned: nothing in the tenant schema or on the surface marks content as excluded from ordinary recall, and `origin_context` remains credential-derived and immutable, so it structurally cannot supply this. The one property of the dropped multi-source axis with no replacement."}
{"id": "imp.per-phase-budget-caps", "capability": "Per-phase consolidation budget caps with individual on/off, instead of one brain-wide budget where ordering is the only cost control", "criticality": "important", "status": "covered", "priority": "p1", "unit": "U11", "source": "important-gaps:consolidation", "notes": "`src/worker/consolidate/estimate.ts:budgetsFor` returns one `Budget` object per model phase, each labelled `consolidate.<phase>` and capped from that phase's own estimate; `cycle.ts` hands each phase only its own. Individual off is a cap of zero, which is also how the free tier is expressed. Every number derives from `src/ai/pricing.ts` — the estimate test recomputes each phase's cap from the canonical table and the routed model's output ceiling, so a constant substituted anywhere in the chain goes red. Starvation is reported rather than silent: the run record carries the stop reason and `CycleResult.phases` names which phase stopped and why."}
{"id": "imp.importance-tiered-selection", "capability": "Importance-tiered selection ahead of the model phases, so truncation degrades by tier rather than randomly", "criticality": "important", "status": "covered", "priority": "p1", "unit": "U11", "source": "important-gaps:consolidation", "notes": "`selectExtractionCandidates` and `selectIngestedPages` order by `salience DESC NULLS LAST` before applying their bound, and the deterministic salience phase runs before every model phase, so the score the ordering reads is this cycle's. That is what makes truncation degrade by importance: a budget that stops the extract phase after N chunks stops it having spent on the N most salient. `test/consolidate/guards.test.ts` pins it with a limit of one over a salient and a dull page inserted dull-first, and asserts the bound rather than a filter left the other out; removing the ordering turns it red."}
{"id": "imp.deterministic-salience", "capability": "Deterministic salience computed above the free-tier line", "criticality": "important", "status": "covered", "priority": "p1", "unit": "U11", "source": "important-gaps:consolidation", "notes": "`computeDeterministicSalience` runs as the fifth deterministic phase, above the free-tier line, from four countable signals: the number of live facts a page states, the number of distinct things those facts are about, recency against the source type's own half-life, and the source-type prior. The last two are imported from `src/core/search/boosts.ts` rather than restated — a second table of per-source-type numbers would be two opinions the brain holds at once about what a chat message is worth. Bounded to [0,1] so the model refinement that may follow is comparable against it rather than living on a different scale."}
{"id": "imp.pattern-artifacts", "capability": "Materialised pattern artifacts from clustering, so 'what keeps coming up' has a read path", "criticality": "important", "status": "not-yet", "priority": "p2", "unit": "U11", "source": "important-gaps:consolidation", "notes": "Half done, and the missing half is the one the row is about. `clusterByEmbedding` runs as the sixth deterministic phase and materialises `content_cluster` + `cluster_member` — greedy agglomeration over the chunk vectors through `withVectorScan`, so hazards H1 and H3 apply here as they do on the read path. What does not exist is the *artifact*: nothing labels a cluster, and no read path answers 'what keeps coming up'. Labelling is a model call and would be a sixth model phase; the read path is a tool-surface change."}
{"id": "imp.fact-invalidation", "capability": "Invalidating derived facts when their source content changes", "criticality": "important", "status": "not-yet", "priority": "p1", "unit": "U11", "source": "important-gaps:consolidation", "notes": "Partly. `markStaleness` marks the superseded version of an upstream item (same `external_ref`, an older `created_at`) and retires the claims it made, matching each to its replacement through the extractor's own supersession key so the two agree with the write path by construction; `collapseDuplicateFacts` retires same-credential duplicates the same way. Both supersede rather than delete, so the chain stays auditable. Still not-yet for the case this note names: a claim **removed** by an edit — present in v1, absent in v2 — has no replacement to point at, and the implementation deliberately leaves it live rather than deleting knowledge on the strength of a filename. That is exactly 'an edit that removes a claim leaves the old fact live', and closing it needs U9's tombstone semantics rather than a heuristic here."}
{"id": "imp.link-reconciliation", "capability": "Link extraction fast enough to answer immediately after being told, and reconciling rather than accumulating — stale edges removed", "criticality": "important", "status": "covered", "unit": "U4", "source": "important-gaps:consolidation", "notes": "`core/write/links.ts:reconcileEdges` runs inside the write transaction as a declared synchronous phase, so the graph answers about what the user just said immediately after they say it. It reconciles rather than accumulates: an edge is live exactly when some live fact still implies it, so an edit that drops a claim removes its edge while an edge two pages attest survives one of them dropping it — and statements retired by supersession are fed in alongside the replaced page's own."}
{"id": "imp.cycle-run-record", "capability": "A per-cycle run record — phases, items, cost, truncation reason — that the brain surface can link to", "criticality": "important", "status": "not-yet", "priority": "p2", "unit": "U11", "source": "important-gaps:consolidation", "notes": "The record exists and is complete: `consolidation_run` carries the trigger, the tier, phases run, model calls, the estimate, the spend, the wall-clock and the truncation reason, with two CHECKs that keep it readable — a finished run that did not dream must say why, and a run that dreamt cannot also carry a truncation reason. `checkpoint.ts:readLatestRun` is its reader. Still not-yet because the row asks for something the brain surface can **link to**, and no tool exposes it; that is a `brain`-tool change and belongs with U14's panel work."}
{"id": "imp.orphan-detection", "capability": "Orphan detection for entities with no inbound edges", "criticality": "important", "status": "not-yet", "priority": "p2", "unit": "U11", "source": "important-gaps:consolidation", "notes": "An orphaned entity is unreachable by the relational arm regardless of how good its embedding is."}
{"id": "imp.algorithm-version-watermarks", "capability": "Algorithm-version watermarks (chunker_version, links_extracted_at) forcing a re-walk of content ingested before a change", "criticality": "important", "status": "not-yet", "priority": "p2", "unit": "U19", "source": "important-gaps:consolidation", "notes": "A ledger row is not honestly 'covered' until existing brains have been re-walked — and a brainz user never runs an upgrade, so the operator must push it."}
{"id": "imp.edge-type-vocabulary", "capability": "A declared edge-type vocabulary with inverses", "criticality": "important", "status": "covered", "unit": "U3", "source": "important-gaps:entity-graph", "notes": "Delivered by U3's schema rather than by U11. `edge_type` in `src/schema/migrations/v2-knowledge-core.sql` is a closed registry, each row declaring its inverse, with a foreign key requiring the inverse to be declared and a constraint trigger asserting the declaration is an involution — so `invested_in ↔ has_investor` cannot become a one-way claim that changes meaning on the second hop. `entity_edge.edge_type` references it, and `core/write/links.ts:PREDICATE_LINKS` maps every extracted predicate onto a declared type or onto no edge at all."}
{"id": "imp.deterministic-edge-sources", "capability": "Model-free deterministic edge sources from connector metadata — sender/recipient, attendees, thread participants", "criticality": "important", "status": "not-yet", "priority": "p1", "unit": "U9", "source": "important-gaps:entity-graph", "notes": "Richer here than upstream and free of model spend, which also gives the free tier a non-empty graph."}
{"id": "imp.entity-resolution-ladder", "capability": "An entity-resolution ladder on the entity tool — precedence, tie-break, per-arm degradation, and how a signed authority file loses to the user's own naming", "criticality": "important", "status": "not-yet", "priority": "p1", "unit": "U5", "source": "important-gaps:entity-graph", "notes": "The ladder now exists on the tool this row names, and the row stays open on measurement rather than on mechanism. `src/mcp/reads.ts:entityCard` resolves on three declared rungs in order — the alias table, the addressing namespace (canonical slugs and redirects, so a retired name still answers), then the canonical name compared raw as the compatibility rung for entities filed with no alias — de-duplicating an id into its highest rung, which is the tie-break, under an in-grant-first ordering that keeps the fence's disclosure for the case it was meant for. The asked name is folded by the shared normalizer (stack.shared-normalizer). The ladder's speculative tail (slug-suffix, mention) is deliberately declined here: those rungs nominate candidates into a ranking, and a tool that returns one card and no ranking would dress a guess as an exact match — the miss path's suggestion list is the honest degradation for that tier. What is still missing: `family.alias.hit1` remains a deferred floor, so the reach of this ladder is unmeasured, and the signed-authority clause has no brainz analogue (oos.skillpacks)."}
{"id": "imp.slug-redirects", "capability": "Slug redirects kept distinct from free-text synonyms", "criticality": "important", "status": "covered", "unit": "U3", "source": "important-gaps:entity-graph", "notes": "Delivered by U3's schema rather than by U17, and the separation is structural rather than conventional: `entity_slug` is the addressing namespace where canonical slugs and redirects share one primary key, so a redirect cannot shadow a live slug and a collision is a unique violation; `entity_alias` is recall vocabulary, many per entity and deliberately not unique across entities. `core/write/links.ts` writes a canonical slug on entity creation and a normalized alias for every surface form. No path creates a `redirect` row yet — merge and rename are U17's — so the primitive is in place ahead of the operation that needs it, which is what this row asked for."}
{"id": "imp.typed-claims", "capability": "Typed claims — metric, value, validity window — so numbers stop being prose", "criticality": "important", "status": "not-yet", "priority": "p2", "unit": "U11", "source": "important-gaps:entity-graph", "notes": "'What rent was I paying and how has it moved' otherwise requires re-reading, which is the exact failure the product exists to remove."}
{"id": "imp.contradiction-verdicts", "capability": "A contradiction verdict vocabulary separating a real conflict from legitimate change over time", "criticality": "important", "status": "not-yet", "priority": "p1", "unit": "U11", "source": "important-gaps:entity-graph", "notes": "Not built, and U11 made it cheaper rather than closing it. The staleness phase runs before the contradiction phase and retires a superseded version's claims, so an ordinary edit is no longer live on both sides when the detector looks — which removes the largest source of the false positives this row is about (Gap #18's cancelled meeting). The prompt also states the distinction. What is still missing is the vocabulary: `contradiction_report.kind` offers `value_conflict`, `temporal_conflict` and `duplicate`, and nothing separates 'these disagree' from 'this replaced that' as a *verdict* a reader or a count can act on."}
{"id": "imp.meeting-mention-scan", "capability": "Meeting back-fill by body mention scan, not only the attendee join", "criticality": "important", "status": "not-yet", "priority": "p2", "unit": "U11", "source": "important-gaps:entity-graph", "notes": "'Who did they say they would introduce me to' is the consumer value."}
{"id": "imp.typed-job-runner", "capability": "A typed job table with more than one job class — consolidate, export, backfill, re-embed — plus re-enqueue on worker death, stall detection and max-attempts dead-lettering", "criticality": "important", "status": "covered", "unit": "U10", "source": "important-gaps:jobs", "notes": "U10 ships it: `control.job` with five typed kinds, lease-fenced claim/complete/fail, reclaim on worker death, a wall-clock attempt deadline for stall detection, and max-attempts dead-lettering. **The reliability machinery is covered; the class list in the capability line is not, and the difference is worth stating.** Exactly one kind has a production handler — `src/worker/serve.ts` registers `consolidate` and nothing else — and exactly one kind is ever enqueued by a running fleet, by `runSchedulerTick`. `export` is declared in `JOB_KINDS` and `LEGAL_TARGETS` with no handler and no enqueuer anywhere in `src/`; the runner is deliberately safe about that, because it scopes its claim to the handler keys it was given and so cannot dead-letter user work it has no code to run. The missing piece is not the job kind: `src/core/export/schedule.ts` runs the export and records the attempt, and its `SelfExportDestination` has no production implementation, so a handler wired today would have nothing to deliver to. `import`, `ingest_pull` and `re_embed` are in the same state — the type set was fixed at U10 on purpose, so consumers landing in later phases do not each re-invent one, and a declared kind is a reservation rather than a claim that the fleet runs it."}
{"id": "imp.lock-renewal-pooled", "capability": "Job lock renewal that survives a pooled connection", "criticality": "important", "status": "covered", "unit": "U10", "source": "important-gaps:jobs", "notes": "`worker/runner.ts` renews leases on a channel of its own and `worker/locks.ts:assertDedicatedLeaseChannel` refuses the wiring that shares one — called at construction in production, not only from a test, because this failure is silent by nature. The steal rule pairs a grace window with a separate attempt deadline, so a starved-but-alive worker is not dispossessed while a wedged one still is, and a steal fences by incrementing `lease_token` rather than asking the loser to stop. Behaviourally guarded by `test/worker/lease-renewal.test.ts` against a real Postgres; `docs/porting-hazards.md` H4 is `guarded`."}
{"id": "imp.poison-job-protection", "capability": "Poison-job protection so one tenant that crashes the worker is not permanently due under debt triggering", "criticality": "important", "status": "covered", "unit": "U10", "source": "important-gaps:jobs", "notes": "U10 ships it: attempts are counted at claim, so a crash loop walks the ladder too; an exhausted job lands in a visible `dead` state and `enqueue` refuses that lane in its own statement until an operator clears it."}
{"id": "imp.pace-concurrency-cap", "capability": "Contention control by concurrency cap rather than scheduling", "criticality": "important", "status": "covered", "unit": "U10", "source": "important-gaps:jobs", "notes": "U10 ships the concurrency bound in the runner, with the capacity arithmetic (`tenants ÷ (ceiling ÷ cycle)`) computed rather than asserted. Contention pacing *within* a bound — the pooler-latency signal — is not ported."}
{"id": "imp.fleet-health-rollup", "capability": "A fleet-side health rollup on the control-plane row", "criticality": "important", "status": "not-yet", "priority": "p2", "unit": "U13", "source": "important-gaps:jobs", "notes": "With one database per tenant nobody opens ten thousand panels, so silent degradation is invisible until a user complains."}
{"id": "imp.job-progress-liveness", "capability": "Job tickets that signal progress and liveness, not only completion", "criticality": "important", "status": "not-yet", "priority": "p2", "unit": "U10", "source": "important-gaps:jobs", "notes": "Liveness ships; progress does not. `control.job` carries `heartbeat_at` and `lease_expires_at` plus a separate `attempt_deadline_at`, which exists precisely because a wedged handler on a healthy worker heartbeats forever — so \"wedged\" is distinguishable from outside. There is still no progress field: nothing on the ticket says how far through a 41,000-item import a job has got."}
{"id": "imp.tool-selection-eval", "capability": "A held-out intent fixture set with strict and lenient scoring, CI-gated — tool descriptions as a versioned, evaluated asset", "criticality": "important", "status": "not-yet", "priority": "p1", "unit": "U7", "source": "important-gaps:protocol", "notes": "The +13-17pp routing result was measured on markdown routing tables, not live tools/list selection, and descriptions freeze into dated manifest epochs."}
{"id": "imp.write-time-dedup", "capability": "Write-time near-duplicate dedup and supersession semantics on remember", "criticality": "important", "status": "covered", "unit": "U4", "source": "important-gaps:protocol", "notes": "`core/write/dedup.ts:classifyStatement` runs synchronously (`phases.ts` puts `dedup` on the sync half precisely so recall cannot return two versions of one claim before a cycle) and returns the frozen contract: `duplicate` with the existing fact id, or `superseded`, which `write-path.ts` records forward-only through `fact.superseded_by` rather than rewriting an immutable-origin row. `mcp/tools/write.ts` surfaces the status on `remember`."}
{"id": "imp.entity-cold-start", "capability": "An honest warm-p99 plus cold_start shape on the entity tool", "criticality": "important", "status": "not-yet", "priority": "p2", "unit": "U6", "source": "important-gaps:protocol", "notes": "Half ships. `entity` returns `cold_start` taken from the connection accessor's own cache miss (`mcp/tenant-db.ts`) plus a measured `latency_ms`, so the flag cannot drift from the thing it describes. The warm-p99 it is meant to be honest *about* is measured nowhere — no committed receipt exists, for the same reason Assumption 5's query-embed p99 does not (see imp.embedding-model-choice)."}
{"id": "imp.volunteered-pointers", "capability": "Volunteered context pointers on the shared response envelope", "criticality": "important", "status": "not-yet", "priority": "p2", "unit": "U6", "source": "important-gaps:protocol", "notes": "The home exists and nothing volunteers into it. `mcp/envelope.ts` ships the bounded `next[]` lane with referential checking against the endpoint's advertised tools, and `remember` uses it for a read-back suggestion — but no read returns pointers to context the caller did not ask for, which is the capability."}
{"id": "imp.protocol-telemetry", "capability": "Protocol telemetry — per-verb counts, error rate, cold/warm latency split, budget-drop rate, entity-hit rate, time-to-first-stored-and-retrieved-memory", "criticality": "important", "status": "not-yet", "priority": "p1", "unit": "U6", "source": "important-gaps:protocol", "notes": "Two inputs exist, no telemetry does. `mcp/access-log.ts` records grant id, tenant id, tool and result class for every call including the refusals (per-verb counts and error rate are derivable from it), and `mcp/control-signals.ts` banks the content-free rank1 sample onto the control-plane row. Missing: the cold/warm latency split, the budget-drop rate, the entity-hit rate, and the 60-second time-to-first-stored-and-retrieved-memory target, which still has no instrument."}
{"id": "imp.content-sanity-gate", "capability": "An ingest-time content-sanity gate with a two-marker quarantine — hidden versus warned-but-searchable", "criticality": "important", "status": "not-yet", "priority": "p1", "unit": "U4", "source": "important-gaps:content-sanity", "notes": "The seam ships; neither the classifier nor the second marker does. `core/write/write-path.ts` accepts a `quarantine` marker, stores the page, hides it from every read, and structurally spends no provider call on it. But the marker is one-valued (hidden) rather than the two this row asks for (hidden versus warned-but-searchable), and the junk classifier that would set it is U9's — so a mailbox's newsletter boilerplate is still embedded, ranked and paid for."}
{"id": "imp.non-text-remember-policy", "capability": "A defined policy and error vocabulary for what remember does with a PDF, a voice memo or a photo", "criticality": "important", "status": "covered", "unit": "U21", "source": "important-gaps:content-sanity", "notes": "`core/media/accept.ts:classifyMedia` is the one classifier, and `mediaPolicyForRemember` is what `remember` answers with: a typed refusal carrying the rejected type, the supported set and the declined families as fields rather than as a sentence. The two refusals are different answers — a voice memo is not readable at all, a screenshot is readable but not through `remember`, which takes text and holds no object store to preserve bytes in — and the message names the door that is open for the second. `media_type` is a declared parameter on the tool (`mcp/tools/index.ts`), because a policy no client can reach is a rule in a module. Guarded by `test/media/remember-policy.test.ts`, including that the declaring statement is not silently stored as prose."}
{"id": "imp.ocr-screenshots", "capability": "OCR-to-text for images, in a model phase rather than on the write path", "criticality": "important", "status": "covered", "unit": "U21", "source": "important-gaps:content-sanity", "notes": "`core/media/ocr-phase.ts:runTranscribePhase` is the first phase of U11's model tier, routed as the `vision` op (KTD13's Image/PDF→text row) with its own per-phase budget and checkpoint. Its output goes through `ingestDocument`, so a screenshot's text is chunked, embedded, fenced and retrieved by the same stack as everything else — no image-vector arm. A PDF carrying a text layer takes `pdf-text.ts` and makes no model call at all, asserted on the transport. Budget exhaustion leaves untranscribed attachments pending rather than lost, and an attachment that transcribes to nothing records the empty string so it is not paid for on every future cycle. The `image_to_text` canary-floor receipt is NOT here: KTD13's exit gate for the consolidation ops is U11's, and grading it needs live paid calls."}
{"id": "imp.raw-payload-preservation", "capability": "Preserving the connector's original payload alongside the rendered page", "criticality": "important", "status": "not-yet", "priority": "p1", "unit": "U9", "source": "important-gaps:content-sanity", "notes": "Without the raw payload, re-deriving with a better extractor after a prompt improvement is impossible fleet-wide."}
{"id": "imp.attachment-storage", "capability": "Attachment and blob storage with a signed-URL story", "criticality": "important", "status": "not-yet", "priority": "p1", "unit": "U21", "source": "important-gaps:content-sanity", "notes": "HALF COVERED, and the half that is missing is the one in the title. Storage is done: `core/media/accept.ts:acceptMedia` preserves the raw payload under the tenant prefix through the `control/storage.ts` accessor, records media type, size and digest on the `attachment` row, refuses to write a row when preservation failed, and round-trips byte for byte (`test/media/accept.test.ts`). The SIGNED-URL story is not: nothing hands a user or a client a time-bounded URL to an attachment, and `recall` returns transcript text rather than a link to the original. The accessor already mints prefix-scoped short-TTL credentials, so the missing piece is a per-object presign plus the surface that returns one — and the surface decision (which tool, what TTL, whether a URL may leave the MCP envelope at all) belongs with U14/U15 rather than here."}
{"id": "imp.storage-tiering", "capability": "Storage tiering, so the export round-trip and the self-host format are not defined over forty thousand machine-generated files", "criticality": "important", "status": "not-yet", "priority": "p2", "unit": "U17", "source": "important-gaps:content-sanity", "notes": "Bound to the open question of whether bulk machine-ingested content is in the export corpus at all."}
{"id": "imp.embedding-model-choice", "capability": "A named embedding model with its measured basis recorded — latency, cost, dimension, and whether it is asymmetric", "criticality": "important", "status": "not-yet", "priority": "p0", "unit": "U20", "source": "important-gaps:content-sanity", "notes": "Deliberately not flipped, and the reason is recorded here so it is not re-litigated by inspection. Three of the four are settled: `text-embedding-3-large` truncated to 1536 through the provider's `dimensions` parameter (KTD8), priced in `src/ai/pricing.ts`, asymmetric by construction (`write/embed.ts:queryEncoding` against `documentEncoding`). The *measured basis* is what is missing — latency is measured nowhere, and Assumption 5's committed query-embed p99 from a deployed container, which U5's own verification list names as a required receipt, does not exist. Both latency and cost are load-bearing on cards already committed, so this stays not-yet until that number is taken."}
{"id": "imp.hnsw-dimension-ceiling", "capability": "Recording pgvector's 2000d HNSW index ceiling as a binding constraint on the embedding choice", "criticality": "important", "status": "covered", "unit": "U3", "source": "important-gaps:content-sanity", "notes": "`schema/vector-index.ts:HNSW_INDEXABLE_DIMENSIONS` records the *index* ceilings (`vector` 2000, `halfvec` 4000) — the numbers that differ from the storage ceilings, which is the whole hazard — and `findIndexableDimensionViolations` scans schema text for any dimensioned declaration above them. Not a test-only guard: `control/migrate.ts` runs it over every rung's DDL, so an embedding swap past the ceiling is refused by the migration rather than by production `CREATE INDEX`."}
{"id": "imp.embedding-provenance", "capability": "A per-page embedding provenance signature, so a same-width model swap re-embeds only affected pages", "criticality": "important", "status": "covered", "unit": "U4", "source": "important-gaps:content-sanity", "notes": "`page.provenance_signature` is a generated column over `embedding_model@dimensions/chunker.normalizer:content_sha256`, and `core/write/write-path.ts` stamps every part on every page — the model id comes from the gateway's own metering record, and a page that encoded nothing fails typed (`embedding_model_unknown`) rather than guessing a model that never ran. The signature is the selector a same-width swap needs; the `re_embed` job kind is declared (`worker/jobs.ts`) but has no handler yet, so nothing consumes it."}
{"id": "imp.last-retrieved-at", "capability": "A usage signal (last_retrieved_at) distinguishing dead content from merely old", "criticality": "important", "status": "not-yet", "priority": "p2", "unit": "U11", "source": "important-gaps:content-sanity", "notes": "Without it there is nothing to prune by on a flat-priced storage bill."}
{"id": "imp.calibration-rows", "capability": "Prediction and outcome rows in the schema so calibration stays possible", "criticality": "important", "status": "not-yet", "priority": "p1", "unit": "U3", "source": "out-of-scope:multi-pack-lenses", "notes": "Still absent: no prediction or outcome table in `src/control/schema.sql` or in the tenant schema. Explicitly must not be dropped by association with the lens packs — it needs schema support, so it is not free to add later."}
{"id": "imp.upgrade-notice", "capability": "A user-visible upgrade moment — what changed, what it did to your memory, and the flag mechanism that staged it", "criticality": "important", "status": "not-yet", "priority": "p2", "unit": "U19", "source": "important-gaps:lifecycle", "notes": "The record and its staging ship; the surface does not, and the capability says *user-visible*. Change records are committed fleet-wide under `upstream/changes/` and validated by `src/upstream/changes.ts`; `src/upstream/change-channel.ts` stages them per tenant off `control.tenant_flag` and renders 'what it did to your memory' from the tenant's own database at read time, storing it nowhere — `test/upstream/change-channel.test.ts` asserts the tenant's own title reaches the rendered block and appears in no textual column of any control-plane table, enumerated from the catalog. What is missing is one line: `brain` embedding the block under `content.changes` in `src/mcp/tools/meta.ts`, which a concurrent unit owns. A function nobody calls is not a user-visible upgrade moment, so this stays `not-yet` until it is wired."}
{"id": "imp.feature-discovery", "capability": "A feature-discovery surface inside the product", "criticality": "important", "status": "not-yet", "priority": "p2", "unit": "U15", "source": "important-gaps:lifecycle", "notes": "With no docs and no CLI, capability discovery is the growth surface."}
{"id": "imp.friction-telemetry", "capability": "Friction and delight telemetry", "criticality": "important", "status": "not-yet", "priority": "p2", "unit": "U15", "source": "important-gaps:lifecycle", "notes": "A chat-panel product is unusually well placed to capture it."}
{"id": "imp.identity-layer", "capability": "An identity layer answering who this person is and what they care about", "criticality": "important", "status": "not-yet", "priority": "p1", "unit": "U12", "source": "important-gaps:lifecycle", "notes": "origin_context answers 'which credential'. This is what makes a briefing personal rather than a query result, and it is the natural content of onboarding."}
{"id": "imp.in-chat-disambiguation", "capability": "A cheap in-chat disambiguation primitive on the response envelope", "criticality": "important", "status": "not-yet", "priority": "p2", "unit": "U6", "source": "important-gaps:lifecycle", "notes": "The envelope has no disambiguation shape (`mcp/envelope.ts:ENVELOPE_KEYS`). The nearest thing is `entityCard`'s `suggestions` on a miss, which answers \"I have not heard of this name\" rather than \"I know two people called this\" — on an ambiguous hit the tool resolves in-grant-first and returns one card without saying it chose. Still costs no tool name."}
{"id": "imp.blocking-canary-split", "capability": "An eval suite split into a deterministic always-blocking half (seeded PRNG, zero model calls) and a sampled model-judged half on the canary", "criticality": "important", "status": "covered", "unit": "U7", "source": "important-gaps:measurement", "notes": "Both halves ship and both are commands. The deterministic half is `bun run eval:blocking` (evals/blocking.ts), wired into the PR-blocking CI job: it runs the tier twice and compares a digest over every per-query outcome, with `fetch` trapped so an accidental live call is a violation rather than a quietly different score. The judged half is `bun run eval:canary` (evals/canary.ts), nightly and secret-gated, routed as the `judge` op with a per-check independence test. The canary grades nothing yet because its producing ops are U11/U12 deliverables, and it refuses rather than reporting a green tick for having graded nothing."}
{"id": "imp.per-question-type-floors", "capability": "Per-question-type accuracy floors — relational, named-entity, temporal, context-fenced — never only an aggregate", "criticality": "important", "status": "covered", "unit": "U7", "source": "important-gaps:measurement", "notes": "All four question-type floors plus the three family floors and the aggregate are enforced by `bun run eval:blocking` on every PR. `evals/gates.ts` refuses to pass a floor it did not measure and treats an under-populated bucket as a violation rather than a skip. Two floors (alias, dilution) report `deferred` while the committed vectors are synthetic; the deferral is derived from the corpus by evals/lexical-reach.ts, printed on every run, and evaporates the day a provider vector lands."}
{"id": "imp.rank1-drift-telemetry", "capability": "Production avg_rank1_score drift as content-free per-tenant quality telemetry", "criticality": "important", "status": "not-yet", "priority": "p1", "unit": "U13", "source": "important-gaps:measurement", "notes": "The only quality signal that works per-tenant across ten thousand databases."}
{"id": "imp.canary-full-path-probe", "capability": "A nightly fixture run through the real MCP surface on the real substrate, doubling as the post-deploy full-path check", "criticality": "important", "status": "not-yet", "priority": "p1", "unit": "U7", "source": "important-gaps:measurement", "notes": "Verifies extensions, GUCs (ef_search is per-connection and can evaporate), index, queue and tenant-string open."}
{"id": "imp.brainz-native-hazard-lane", "capability": "A hazard lane for failure modes brainz discovers on its own substrate, with no upstream analogue", "criticality": "important", "status": "not-yet", "priority": "p1", "unit": "U3", "source": "important-gaps:measurement", "notes": "`docs/porting-hazards.md` opens by defining itself as \"failures gbrain already paid for\", and all four cards (H1–H4) are ported ones; `test/hazards/registry-consistency.test.ts` enforces the card-to-stub correspondence but knows nothing about a hazard's origin. There is still no lane, and no card, for a failure brainz finds on its own substrate — H1's `SET LOCAL` over a connection LRU remains the example with nowhere to go."}
{"id": "imp.privacy-scanners", "capability": "The privacy scanner guards ported as CI checks", "criticality": "important", "status": "not-yet", "priority": "p2", "unit": "U16", "source": "important-gaps:measurement", "notes": "Still unported, and now counted. U19's guard sweep gives them a card — H5 in `docs/porting-hazards.md`, answering for all six upstream scanners at once — with a skipped stub in `test/hazards/swept.test.ts`, so `bun test` prints the gap on every run instead of it living only here. They matter more for a product holding real people's mail than they did upstream."}
{"id": "oos.code-indexing", "capability": "Code indexing end to end — tree-sitter chunking across 36 grammars, call and import graph, six code tools, a code-specific embedding model", "criticality": "optional", "status": "omitted", "reason": "There is no repository ingestion surface; the upstream project carries this because it is a coding-agent mod. Under the seven-tool freeze these six tool names could never be added without a manifest epoch, so this is a permanent decision rather than a deferral.", "reason_code": "product:consumer-not-operator", "revisit_by": "2027-08-31", "source": "out-of-scope"}
{"id": "oos.llm-query-expansion", "capability": "LLM multi-query expansion on the read path", "criticality": "optional", "status": "omitted", "reason": "A server-paid model call per query — the exact shape the tool surface was cut to eliminate. The measured lift is barely above noise.", "reason_code": "product:read-path-no-llm-spend", "revisit_by": "2027-06-30", "source": "out-of-scope"}
{"id": "oos.llm-cross-modal-tiebreak", "capability": "LLM cross-modal tie-break", "criticality": "optional", "status": "omitted", "reason": "Per-query server-paid model call, and there is no modality left to disambiguate once images land as OCR-to-text.", "reason_code": "product:read-path-no-llm-spend", "revisit_by": "2027-06-30", "source": "out-of-scope"}
{"id": "oos.chunker-modes", "capability": "Semantic and LLM topic-shift chunking exposed as user-visible modes", "criticality": "optional", "status": "omitted", "reason": "Zero-setup means one right answer, not a mode picker. The chunker choice itself is still quality-affecting and must be picked deliberately and recorded — see gap.write-path.", "reason_code": "product:zero-setup", "revisit_by": "2027-06-30", "source": "out-of-scope"}
{"id": "oos.search-mode-picker", "capability": "The three-mode search picker", "criticality": "optional", "status": "omitted", "reason": "Delete the picker, not the decision — the knobs still need values, chosen once and centrally.", "reason_code": "product:zero-setup", "revisit_by": "2027-06-30", "source": "out-of-scope"}
{"id": "oos.lens-packs", "capability": "Multi-pack lenses and agent-authored schema evolution", "criticality": "optional", "status": "omitted", "reason": "Power-user ontology curation contradicts zero setup. Consequence recorded: taxonomy growth becomes a centrally owned release job. Calibration is a separate question and is NOT dropped with it — see imp.calibration-rows.", "reason_code": "product:consumer-not-operator", "revisit_by": "2027-06-30", "source": "out-of-scope"}
{"id": "oos.skillpacks", "capability": "Brain-resident skillpacks, the third-party quality rubric, endorsed/community tiering and the install nag", "criticality": "optional", "status": "omitted", "reason": "Defends a supply chain that does not exist here — nothing is installable, so there is no install path to attack. One property is lifted rather than dropped: the brain ships the instructions for using it and they travel to whichever client connects (gap.server-instructions).", "reason_code": "product:no-install-surface", "revisit_by": "2027-06-30", "source": "out-of-scope"}
{"id": "oos.operator-machinery", "capability": "The 52-skill markdown router, steerable background subagents, the CLI, and the five-page operator SPA", "criticality": "optional", "status": "omitted", "reason": "Operator machinery for a product with no operator; the host's own tool selector is the router. The durable job substrate underneath the subagents is NOT dropped — see imp.typed-job-runner.", "reason_code": "product:consumer-not-operator", "revisit_by": "2027-06-30", "source": "out-of-scope"}
{"id": "oos.brain-score", "capability": "The 0-100 composite brain_score", "criticality": "optional", "status": "omitted", "reason": "A number with no attached action. The sub-scores — embedding coverage, orphan rate, dead-link rate — are kept as internal fleet telemetry instead.", "reason_code": "product:no-action-attached", "revisit_by": "2027-03-31", "source": "out-of-scope"}
{"id": "oos.citation-rot-repair", "capability": "Citation-rot repair", "criticality": "optional", "status": "omitted", "reason": "Low consumer value. The transferable piece — the confidence-gated mutation policy — is kept under gap.unverified-model-output.", "reason_code": "product:consumer-not-operator", "revisit_by": "2027-06-30", "source": "out-of-scope"}
{"id": "oos.expert-routing", "capability": "Expert routing (find_experts)", "criticality": "optional", "status": "omitted", "reason": "No input for it in a single-person brain.", "reason_code": "product:no-input", "revisit_by": "2027-06-30", "source": "out-of-scope"}
{"id": "oos.generative-surface", "capability": "The generative surface — brainstorm and free-association tools", "criticality": "optional", "status": "omitted", "reason": "Low consumer value and server-paid generation. The transferable piece — the selection-fan-out hazard behind the runaway-cost incident — is kept under imp.importance-tiered-selection.", "reason_code": "product:read-path-no-llm-spend", "revisit_by": "2027-06-30", "source": "out-of-scope"}
{"id": "oos.embedding-gateway-breadth", "capability": "A 21-provider embedding gateway with a per-provider column resolver", "criticality": "optional", "status": "omitted", "reason": "One hosted answer needs one provider. The asymmetric query/document detail inside it is NOT optional and is kept under stack.vector-arm.", "reason_code": "product:one-hosted-answer", "revisit_by": "2027-06-30", "source": "out-of-scope"}
{"id": "oos.multi-source-axis", "capability": "The multi-source axis within one brain", "criticality": "optional", "status": "omitted", "reason": "Deliberately replaced by credential-derived context. One property has no replacement and is kept as a separate row: user-designated content excluded from ordinary recall (gap.user-excluded-content).", "reason_code": "product:zero-setup", "revisit_by": "2027-03-31", "source": "out-of-scope"}
{"id": "oos.image-embedding-arm", "capability": "An image-embedding retrieval arm — searching pictures by visual similarity rather than by their text", "criticality": "optional", "status": "omitted", "reason": "U21 lands images as OCR-to-text instead, and the plan's scope boundary declines the arm outright: the dominant consumer image is a screenshot, which is mostly text, so a text arm answers the query that is actually asked ('find the screenshot with the wifi password') and a visual arm answers one nobody has asked yet. Adopting it later is not a migration — KTD9 reserved `attachment.image_embedding vector(1152)` at provision time, so the column exists, is unindexed and is unread, and the day something reads it its entry in `schema/vector-index.ts:RESERVED_VECTOR_COLUMNS` moves. What would settle a revisit: a query log showing recall failures that OCR text cannot serve, and a model whose price and dimension fit the reserved column.", "reason_code": "product:ocr-instead-of-image-vectors", "revisit_by": "2027-06-30", "source": "out-of-scope"}
{"id": "imp.vision-seat-empty-result", "capability": "The routed vision seat actually returning transcribed text", "criticality": "important", "status": "not-yet", "priority": "p0", "unit": "U21", "source": "u21-model-choice", "notes": "Observed live on 2026-08-16, against the account's own Unified Billing endpoint: every documented input form for `@cf/moondream/moondream3.1-9B-A2B` returns `{\"result\":{}}` — HTTP 200, `success: true`, no `answer`, no `caption`, no `metrics`, no usage block. Public HTTPS URL and base64 data URI, `question` and the schema-default question, `task: query` and `task: caption`, reasoning on and off: all empty. The control that makes this a finding about the seat rather than about the request is in the same recorded set — `@cf/llava-hf/llava-1.5-7b-hf`, same account, same `/run/` path, same minute, answered 'The text \"Google\" appears in the image.' Both bodies are committed at `test/ai/recorded-cloudflare-shapes.ts`. THE FAILURE THIS IS FILED AGAINST, and the reason it is p0 rather than a note: an empty response is the transcription prompt's OWN protocol for 'this image contains no legible text' (`src/core/media/ocr-phase.ts`), so a seat that returns nothing for everything would write an empty `ocr_text` for every screenshot in every brain and never queue the attachment again — permanent silent loss, paid for. It does not, because the missing usage block makes it `usage_unreported` first, which leaves the attachment queued; `test/ai/reasoning-outcome.test.ts` pins that ordering explicitly. WHAT FLIPS THIS ROW: one call to the routed id returning a non-empty `answer` with a `metrics` block, plus the transcription accuracy A/B against the committed gold at `evals/fixtures/transcription.ts` that `oos.moondream-screenshot-specialist` always owed. If the seat stays empty, the decision to re-take is the seat itself — the licence bar rules out the llama row, so the candidate list is the rest of Cloudflare's image-to-text catalog."}
{"id": "oos.moondream-screenshot-specialist", "capability": "moondream3.1-9B-A2B as the screenshot-specialist vision model, in place of llama-3.2-11b-vision-instruct", "criticality": "optional", "status": "covered", "unit": "U21", "source": "u21-model-choice", "notes": "The swap is done: `src/ai/routing.ts` routes the `vision` op to `@cf/moondream/moondream3.1-9B-A2B` on both the price and the licence. The price blocker this row was declined on is retired — Cloudflare's own model catalog publishes $0.30/M input and $1.00/M output, entered in `src/ai/pricing.ts` and asserted in `test/ai/cloudflare-seats.test.ts`, so it is no longer a per-phase cap over a cost nobody can compute. The licence argument now runs the other way too: the llama seat it replaces requires submitting the prompt 'agree' to Meta's licence and representing non-EU domicile, which a hosted plane cannot accept on its users' behalf, so no profile routes it and `test/ai/cloudflare-seats.test.ts` asserts none does. The transport speaks the model's own published schema rather than the chat wire (`/run/{modelId}`, `question` not `prompt`, `image` as a base64 data URI) because the OpenAI-compatible path refuses it outright. What this row does NOT claim is that the seat produces transcriptions today — see `imp.vision-seat-empty-result`, which carries that separately rather than letting a routed-and-broken seat read as a working one."}
{"id": "oos.glm-5-3-judge-upgrade", "capability": "GLM-5.3 in the judge seat, in place of glm-5.2, for the model-graded eval metrics", "criticality": "optional", "status": "omitted", "reason": "Z.ai shipped GLM-5.3 on 2026-08-14 and it looks strictly better than the seat we run: same base model, all gains from scaled post-training, improvements on nine benchmarks and regressions on none — including two that are not coding-specific and would plausibly transfer to grading (HLE-with-tools 54.7 -> 62.5, GDPVal-AA v2 Elo 1508 -> 1769). It is blocked, not rejected: Cloudflare has not added it (the catalogue and the Workers AI changelog both still show @cf/zai-org/glm-5.2 from 2026-06-16 as the newest GLM), and Z.ai is holding the weights ~2 weeks pending safety hardening, so CF cannot host it yet. Routing to Z.ai directly instead would break the CF-hosted-only rule and add an R10 subprocessor that docs/legal/subprocessors.md has already published. What would settle it: @cf/zai-org/glm-5.3 appearing in the Workers AI catalogue with a published per-million price, entered in the canonical table, plus U13's cost-down A/B confirming the gains survive a harness we control — the published numbers are vendor-reported and deserve the same independent-harness scepticism that sank the DeepSeek comparison. A same-vendor, same-base-model swap is the low-risk one: the judge's prompt contract and output-parse shape should carry over, where a cross-vendor swap risks changing verdict formatting and re-scoring every committed receipt for reasons unrelated to quality. Then it is one row in routing.ts.", "reason_code": "vendor:not-hosted-upstream", "revisit_by": "2026-11-30", "source": "u11-model-choice"}
{"id": "oos.deepseek-v4-judge-challenger", "capability": "DeepSeek V4 Flash or V4 Pro in the judge seat, in place of glm-5.2", "criticality": "optional", "status": "omitted", "reason": "Evaluated 2026-08-15 and declined — both variants, for opposite reasons. V4 Flash (@cf/deepseek-ai/deepseek-v4-flash-0731, $0.440/$1.320 per M) is 69% cheaper blended but one point down on the Artificial Analysis Intelligence Index (52 vs 53) and slower on both measured axes (120.7 vs 127.1 tok/s; TTFT 1.43s vs 1.39s). V4 Pro (@cf/deepseek-ai/deepseek-v4-pro-0813, $1.320/$3.960) is at parity on the index (53 vs 53) but 41% slower generating and 29% worse to first token, for a 7% saving — and note the saving is ~22% on DeepSeek's own API and shrinks to 7% through Cloudflare's pricing, so the one reason to switch is weakest exactly where the CF-hosted-only constraint puts us. Neither is supported by evidence relevant to this seat: no IFBench, GPQA or long-context-reasoning numbers are published for either pair, and every comparable benchmark is coding or agentic, while the judge grades agreement with gold answers on extraction and briefing quality. DeepSeek's headline 1.7-point Terminal-Bench win was measured on an unshipped in-house harness nobody outside could run, and the smallest documented harness-only effect in the 2026 literature is 2.1 points — the claimed margin is inside the measurement error. Standing bar for any judge swap: the judge is the one op where a model change silently rewrites every committed score, so price alone never justifies it; it needs U13's cost-down A/B against gold, which is also the only measurement that is not the judge grading its own replacement.", "reason_code": "vendor:no-quality-evidence-for-seat", "revisit_by": "2027-02-28", "source": "u11-model-choice"}
{"id": "gap.cloudflare-embedding-seat", "capability": "The embedding seat on Cloudflare, so one credential pays for every model op", "criticality": "optional", "status": "not-yet", "priority": "p2", "unit": "U8", "source": "u21-model-choice", "notes": "The one seat that did NOT move when the other eight went onto Cloudflare's Unified Billing, and the only reason it is a row rather than an edit is that it is not a routing change at all. `@cf/qwen/qwen3-embedding-0.6b` returns 1024 dimensions; the stored column is `vector(1536)` in `src/schema/tenant.sql` and again in `src/schema/migrations/v2-knowledge-core.sql`; and the `dimensions` parameter is IGNORED on this endpoint — asking a 1024d model for 768 returns 1024 — so KTD8's 'truncation belongs to the provider's parameter, never to client-side slicing' has nothing to reach for. Flipping the routing row alone makes every embed call fail `embedding_dimension_mismatch`. TWO blockers, and the second is the one that matters: even a width-compatible model would be a different vector space, so every chunk already embedded by `text-embedding-3-large` would be incomparable to everything written after the switch — a silent recall collapse with no error anywhere, which is the same failure class `h2-missing-vector-index` exists against. The price is already verified and entered in `src/ai/pricing.ts` ($0.0118/M) precisely so that the number lives in the one file allowed to hold one rather than in a plan; nothing routes to it and `test/ai/cloudflare-seats.test.ts` asserts the seat is still OpenAI's. WHAT FLIPS THIS ROW: an expand-only rung adding a second vector column at the new width beside the existing one, a re-encode of every chunk in every brain through `evals/regenerate-embeddings.ts` applying the same contextual wrap, a cutover that reads the old column until the new one is complete per tenant, and a retrieval A/B showing the new space is not worse. Until then the hosted plane holds one OpenAI credential for this seat alone, which is what `hostedKeys` in `src/fleet/compose.ts` still exists to carry."}
{"id":"oos.llama-vision-licence-bar","capability":"Cloudflare-hosted `@cf/meta/llama-3.2-11b-vision-instruct` in the vision seat — KTD13's original Image/PDF-to-text pick","criticality":"optional","status":"omitted","reason":"Declined on the licence, not on price or quality, and filed as its own row so the bar survives the seat it currently protects. Cloudflare gates this model behind Meta's Llama 3.2 Community Licence: access requires submitting the literal prompt `agree` AND representing that the user is not domiciled in the EU. A hosted plane cannot make the second representation on its users' behalf — brainz does not know where a tenant lives, would be asserting it for people who never saw the term, and the beta's reachable stranger includes EU residents by design. That is a representation, not a checkbox, so no amount of routing care makes it accurate. WHAT ENFORCES IT TODAY, so this row is a record rather than a hope: no profile routes the id and `test/ai/cloudflare-seats.test.ts` asserts none does; and the id carries no entry in `src/ai/pricing.ts`, so `findRoutingFaults` in `src/ai/routing.ts` refuses any non-self-host route to an unpriced model at module scope — re-adding the price is the act that would unlock the seat, and it is the change a reviewer should stop. THE SELF-HOST CARVE-OUT IS NOT AN EXCEPTION TO THIS: `SELF_HOST_PROFILE` routes `self-host/llama-3.2-11b-vision-instruct`, a different id on the operator's own hardware, where the operator accepts Meta's terms in their own name about their own domicile — a representation they can actually make. The bar is on serving those weights TO STRANGERS from a plane that cannot speak for them. WHAT WOULD SETTLE A REVISIT: Meta dropping the EU-domicile term, or a per-tenant attested domicile the tenant themselves asserts, plus an operator entity willing to carry the licence. Note the live pressure this row is standing against — `imp.vision-seat-empty-result` has the routed moondream seat returning `{}` for every documented input, so the seat this bar rules out is exactly the one somebody under time pressure will reach for. The candidate list when that happens is the rest of Cloudflare's image-to-text catalog, not this row.","reason_code":"vendor:licence-terms-unacceptable","revisit_by":"2027-06-30","source":"u21-model-choice"}
{"id":"gap.register-passthrough-vendor-blindness","capability":"The register completeness check seeing a lab reached through a passthrough model id, not just through a provider field or a hostname","criticality":"important","status":"not-yet","priority":"p1","unit":"U16","source":"u21-model-choice","notes":"Found while re-checking the published trust story after the seats moved, and it is a guard that can point the WRONG WAY rather than merely fall silent. `src/register/completeness.ts` derives its provider evidence from `Route.provider`. Since the move, the three Google seats on the hosted profile carry `provider: cloudflare` and a Cloudflare hostname; the serving lab is encoded in the model id string (`google/gemini-3.5-flash-lite`) and appears in none of the three evidence sets. THE VIOLATING CASE, run rather than reasoned: reduce the evidence to the hosted profile alone (drop the self-host provider set and `generativelanguage.googleapis.com` from the host sweep) and `findRegisterGaps` returns `google claims the provider \"google\", which no routing profile can reach any more` — it reports the Google entry as STALE and invites its deletion, while every extracted, enriched and contradiction-checked document keeps crossing to Google through the passthrough. The entry survives today only by accident of the self-host profile still reaching Google directly and still naming its hostname in source; nothing about the hosted plane holds it in place. The repo already has the function that knows better — `evals/canary.ts:servingOrgOf` reads the vendor out of a passthrough id, and `test/ai/cloudflare-seats.test.ts` asserts `servingOrgOf` of the hosted extract seat is `google` — so the fact is available and simply is not wired into the register sweep. WHAT WOULD CLOSE IT: a fourth evidence set in `src/register/completeness.ts` reducing every shipped profile through the serving-org of its routed id, so a passthrough lab is claimed evidence in the same way a provider id is, plus a both-directions test (an unregistered passthrough lab is a finding; a register entry claiming a lab no id serves is a finding). Deliberately NOT closed in the same pass that found it: the sweep is shared with concurrent work and the finding is worth more written down than raced. `docs/legal/subprocessors.md` states the limitation in the published surface meanwhile, which is the half a reader of that document needs."}
